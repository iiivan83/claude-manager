# Отчёт код-ревью

**Дата:** 30.03.2026
**Время:** 01:59

## Проход 1: Качество кода

**Найдено проблем:** 11
**Исправлено:** 11

- **main.py, строки 22-28:** неиспользуемая константа `TELEGRAM_COMMANDS` — дубль `BOT_COMMANDS` из `bot.py`. Нигде не импортируется и не используется. Удалена. Тест удалённой константы в `test_main.py` тоже удалён (эквивалентный тест `BOT_COMMANDS` уже есть в `test_bot.py`).

- **bot.py, `_send_telegram_message` (64 строки):** превышение лимита 20 строк. Разбита на 4 функции: `_send_raw` (вызов Telegram API), `_fallback_to_plain_text` (обработка BadRequest), `_handle_retry_after` (ожидание RetryAfter), `_handle_network_error` (логирование сетевых ошибок). Основная функция сократилась до 15 строк.

- **bot.py, `_download_and_save_file` (30 строк):** превышение лимита. Логика извлечения file_id/расширения из сообщения вынесена в `_extract_file_info`. Основная функция сократилась до 13 строк.

- **bot.py, `_send_to_claude_and_respond` (75 строк):** значительное превышение лимита. Разбита на 3 функции: `_ensure_process_running` (создание процесса при необходимости), `_handle_claude_result` (обработка результата: обновление ID, отправка ответа), `_send_to_claude_and_respond` (оркестрация). Каждая в пределах 20 строк.

- **bot.py, `_clean_old_received_files` (31 строка):** превышение лимита. Логика проверки возраста файла вынесена в `_is_file_expired`. Основная функция сократилась до 19 строк.

- **bot.py, `setup_bot` (44 строки):** превышение лимита. Регистрация обработчиков вынесена в `_register_handlers`. Основная функция сократилась до 13 строк.

- **process_manager.py, `_process_events` (54 строки):** превышение лимита. Проверка отмены вынесена в `_check_stop_requested`, обработка прогресса — в `_handle_progress_event`. Сдвоенный возврат SendResult (для got_result и not got_result) объединён в единый return. Основная функция сократилась до 20 строк.

- **process_manager.py, `_retry_loop` (77 строк):** значительное превышение лимита. Одна попытка ретрая вынесена в `_execute_single_retry`, формирование финального результата — в `_build_exhausted_result`. Основная функция сократилась до 20 строк.

- **process_manager.py, `send_message` (44 строки):** превышение лимита. Валидация процесса вынесена в `_validate_process_ready`, подготовка флагов — в `_prepare_for_send`. Основная функция сократилась до 15 строк.

- **session_reader.py, `get_recent_sessions` (41 строка):** превышение лимита. Логика проверки пути и получения файлов вынесена в `_list_sorted_session_files`. Основная функция сократилась до 12 строк.

## Проход 2: Безопасность

**Найдено проблем:** 0
**Исправлено:** 0

Все пункты чеклиста пройдены:
- Токены только в `.env`, загружаются через `config.py`
- Белый список проверяется в каждом из 8 обработчиков
- HTML-экранирование через `html.escape()` в `message_splitter.py`
- Нет command injection: используется `asyncio.create_subprocess_exec` (список аргументов)
- Нет path traversal: имена файлов генерируются программно, пользователь не контролирует путь

## Проход 3: Архитектура

**Найдено проблем:** 0
**Исправлено:** 0

Все пункты чеклиста пройдены:
- Слоёная архитектура соблюдена: транспорт -> бизнес-логика -> инфраструктура -> утилиты
- Бизнес-логика не импортирует Telegram или процессы
- Инфраструктура не импортирует бизнес-логику
- Все I/O через async/await, файловые операции через `asyncio.to_thread()`
- Нет `time.sleep()`, только `asyncio.sleep()`
- Атомарная запись JSON через `.tmp` + `os.replace()`
- Нет deadlocks: блокировки (`asyncio.Lock`) используются в одном порядке

## Проход 4: Соответствие BRD

**Найдено проблем:** 0
**Исправлено:** 0

Все CJM реализованы:
- **CJM-01** (Первый запуск): config.py + main.py
- **CJM-02** (Текстовое сообщение): bot.py handle_message -> _send_to_claude_and_respond
- **CJM-03** (Фото/документ): bot.py handle_photo + handle_document
- **CJM-04** (/new): bot.py handle_new
- **CJM-05** (/sessions): bot.py handle_sessions
- **CJM-06** (/N): bot.py handle_switch_session
- **CJM-07** (/all): bot.py handle_all + session_watcher.py
- **CJM-08** (/stop): bot.py handle_stop + process_manager.stop_process
- **CJM-09** (Двойной запуск): main.py _acquire_lock + Conflict

Все сквозные механизмы из BRD работают:
- Проверка доступа при каждом действии
- Промежуточные обновления с throttle 30 секунд
- Координация watcher/handler через pause/resume
- Дневная нумерация сессий
- Восстановление состояния при перезапуске
- Автоочистка received_files/

Решения из brd-validation-report учтены:
- /N ищет и в реестре, и среди видимых сессий
- /stop прерывает ретраи
- /new сразу запускает процесс
- При перезапуске без привязки — режим /all

## Повторная проверка

**Найдено новых проблем:** 1
**Исправлено:** 1

- **bot.py, `_handle_bad_request`:** при рефакторинге `_send_telegram_message` функция `_handle_bad_request` содержала голый `raise` вне контекста `except`. Это привело бы к `RuntimeError` при вызове с `parse_mode != HTML`. Переписана как `_fallback_to_plain_text`, возвращающая `bool`, а `raise` перенесён обратно в `except BadRequest` блок `_send_telegram_message`.

## Итого

- **Всего проблем найдено:** 12
- **Всего исправлено:** 12
- **Тесты:** все 383 зелёные (1 тест удалён — тестировал удалённый дубль константы)
- **Нерешённые проблемы:** нет
