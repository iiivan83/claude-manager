# Fix Process: после /new ответ от новой сессии не приходит

**Дата запуска:** 30-03-2026 04:00
**Исходный отчёт:** `development/docs/root-cause-reports/30-03_03-20_new-session-no-response.md`

## Фаза 1: Разбор и верификация

- **Всего рекомендаций:** 4
- **Принято:** 1
- **Отложено:** 3 (пользователь решил применить отдельно)

### Детали
1. **[CODE] Убрать `create_process` из `handle_new`** — ПРИНЯТО
   - Обоснование: устраняет гонку между стартом процесса и отправкой сообщения
2. **[CODE] Таймаут в `read_events`** — ОТЛОЖЕНО
3. **[CODE] Закоммитить `--resume` fix** — ОТЛОЖЕНО
4. **[ARCHITECTURE] Принцип таймаутов в CLAUDE.md** — ОТЛОЖЕНО

## Фаза 2: Исправления

### Изменение 1: Убрать `create_process` из `handle_new`
- **Тег:** [CODE]
- **Скилл:** прямое редактирование агентом
- **Статус изменения:** УСПЕХ
- **Что сделано:**
  - В `bot.py`: убрана строка `await process_manager.create_process(session_id)` из `handle_new`. Теперь `/new` только создаёт привязку и отправляет подтверждение. Процесс Claude CLI запустится при первом сообщении через `_ensure_process_running`.
  - В `test_bot.py`: из теста `test_handle_new_creates_session` убраны мок и проверка `create_process`.
- **Файлы затронуты:**
  - `src/claude_manager/bot.py`
  - `tests/test_bot.py`

## Фаза 3: Итоги

- **Успешно исправлено:** 1 из 1
- **Ошибки:** 0
- **Тесты:** 381 passed, 0 failed
- **Исходный отчёт:** перемещён в `resolved/`
