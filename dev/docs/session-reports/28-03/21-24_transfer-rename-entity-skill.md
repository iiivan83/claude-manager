# Сессия 28-03: перенос скилла rename-entity из sumainmaster

## Резюме

Перенесён скилл `rename-entity` из проекта sumainmaster в claude_manager с адаптацией области поиска под структуру текущего проекта. Скилл готов к использованию.

## Изменённые файлы

- `.claude/skills/rename-entity/SKILL.md` — создан — скилл переименования сущностей проекта с автоматическим исправлением ссылок, адаптирован из sumainmaster

## Решения

- **Замена `migration/` → `development/`**: в оригинальном скилле область поиска ссылок — `migration/` и `.claude/`. В claude_manager аналог `migration/` — это `development/` (документация, спецификации, отчёты, скрипты). Остальная логика скилла универсальна и перенесена без изменений.

## Контекст для следующей сессии

Скилл `rename-entity` перенесён и доступен. Исходный проект: `/Users/ivan/Desktop/claude-sandbox/su-main-master 2`. Скилл ищет и заменяет ссылки только в `development/` и `.claude/` — если понадобится расширить область (например, на `src/`), нужно обновить секцию "Область действия" в SKILL.md.
