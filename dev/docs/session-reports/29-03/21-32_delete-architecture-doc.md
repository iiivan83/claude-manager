# Сессия 29-03: удаление архитектурного документа

## Резюме

Удалён файл `development/docs/project_architecture.md` и вычищены все живые ссылки на него в документации проекта. Сессионные отчёты (исторические записи) не тронуты.

## Изменённые файлы

| Файл | Действие | Что сделано |
|------|----------|-------------|
| `development/docs/project_architecture.md` | удалён | Полностью удалён архитектурный документ проекта |
| `development/docs/docs-index.md` | изменён | Убрана секция `## development/docs/project_architecture.md` (строки 11-19: описание + 5 пунктов "Читать если") |
| `development/specs/pipeline-spec.md` | изменён | Убраны 7 ссылок на `project_architecture.md`: из входных данных S8 (строка 680), из выхода S8 (685), из алгоритма S8 (691-692), из промпта S8 (710), из структуры `pipeline-state.json` (835), из ворот качества фазы 7 (1166), из аргументов оркестратора (1281) |

## Решения

- **Решение**: сессионные отчёты не редактировать. **Причина**: это исторические записи о прошлых сессиях, менять их — переписывать историю.
- **Решение**: общие фразы вроде "архитектурный документ" в pipeline-spec.md оставить. **Причина**: это абстрактные описания входных данных пайплайна, не ссылки на конкретный файл.

## Проблемы и решения

- **Проблема**: одна строка в pipeline-spec.md (691) содержала нестандартную кодировку кириллицы, Edit tool не смог найти точное совпадение. **Решение**: использован Python-скрипт для удаления строки по номеру и перенумерации списка.

## Контекст для следующей сессии

Архитектурный документ удалён. Архитектурные принципы по-прежнему описаны в CLAUDE.md (секция "Архитектурные принципы"). В pipeline-spec.md скилл `update-project-docs` (S8) больше не обновляет архитектурный документ — только CLAUDE.md, deployment-guide.md и docs-index.md.
