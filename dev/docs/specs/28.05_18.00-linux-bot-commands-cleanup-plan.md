# Linux-only: чистка macOS-хвоста — план реализации

> **Для исполнителя.** План разбит на мини-шаги (2-5 минут каждый). Стиль: TDD — сначала падающий тест, потом реализация, потом коммит. Чекбоксы `- [ ]` — для отметки прогресса.

**Парная спека:** `dev/docs/specs/28.05_17.25-linux-bot-commands-cleanup-spec.md`

**Цель.** Убрать весь macOS-зависимый код, инфраструктуру и документацию из проекта `claude-manager`. После работы команда `/restart` корректно перезапускает бота через `systemctl --user`, логи пишутся в XDG-стандартный путь, `PROJECTS_ROOT_DIR` обязателен, shell-скрипты работают только под Linux, документация описывает только Linux.

**Архитектура.** Точечные правки трёх Python-модулей, полная перезапись трёх shell-скриптов под Linux, обновление двух тестовых файлов (Python + bash), переписывание двух документов и точечная чистка `CLAUDE.md`. Самоперезапуск `/restart` — детач через `asyncio.create_subprocess_exec("bash", "-c", "sleep 2 && systemctl --user restart ...")` с `start_new_session=True`. Принцип verify-before-and-after сохраняется в `restart-claude-manager.sh` (preflight + post-flight через `systemctl is-active` + `pgrep`).

**Стек.** Python 3.13 (asyncio, subprocess), bash 5+, systemd user services, `inotify-tools` для watcher, pytest + pytest-mock для тестов, journalctl для журналирования сервиса.

---

## Фаза 0. Baseline и подготовка

### Task 0: Baseline-прогон тестов

**Files:**
- Read-only: вся папка `tests/`

- [x] **Step 1: Зафиксировать baseline**

Run:
```bash
cd /home/ivan/claude-sandbox/claude_manager
source .venv/bin/activate
python -m pytest tests/ -v --ignore=tests/e2e 2>&1 | tail -40
```

Expected: либо все 921 тест зелёные, либо известный список падений. Записать падения, чтобы после правок было понятно, что новые регрессии от наших правок, а не унаследованные.

- [x] **Step 2: Зафиксировать падения в комментарии под этим шагом**

Записать в комментарий внутри плана либо отдельно: «baseline: X passed, Y failed, список падений: ...».

> **Baseline (зафиксирован 28.05 в ходе ревью):** 1009 passed, 6 skipped, **1 failed** —
> `tests/integration/test_session_lifecycle.py::TestFilePersistence::test_registry_survives_reload`.
> Падение унаследованное: тест ожидает запись без поля `summary`, а реальный реестр пишет
> `{"session_id": ..., "backend": ..., "summary": ""}`. Воспроизводится на чистом HEAD без
> правок `bot.py`/`test_bot.py` (проверено через `git stash`). Это **не** регрессия от
> Фазы 1–3, к этой работе не относится — чинить отдельно.

---

## Фаза 1. `src/claude_manager/config.py` — удаление DEFAULT_PROJECTS_ROOT

### Task 1: Падающий тест на ConfigError для пустого PROJECTS_ROOT_DIR

**Files:**
- Modify: `tests/test_config.py`

- [x] **Step 1: Добавить тест в класс `TestResolveProjectsRoot`**

Открыть файл, найти класс `TestResolveProjectsRoot` (строка 259), добавить новый метод **внутри класса**:

```python
    def test_none_raises_error(self) -> None:
        """None — больше не используется default-путь, ConfigError с упоминанием переменной."""
        with pytest.raises(ConfigError, match="PROJECTS_ROOT_DIR"):
            _resolve_projects_root(None)

    def test_empty_raises_error(self) -> None:
        """Пустая строка — больше не используется default-путь, ConfigError."""
        with pytest.raises(ConfigError, match="PROJECTS_ROOT_DIR"):
            _resolve_projects_root("")
```

- [x] **Step 2: Запустить новые тесты — убедиться, что они падают**

Run:
```bash
python -m pytest tests/test_config.py::TestResolveProjectsRoot::test_none_raises_error -v
python -m pytest tests/test_config.py::TestResolveProjectsRoot::test_empty_raises_error -v
```

Expected: оба теста FAIL — на текущей реализации `None` и `""` возвращают абсолютный путь default-а, а не поднимают ConfigError.

### Task 2: Удалить DEFAULT_PROJECTS_ROOT и переписать `_resolve_projects_root`

**Files:**
- Modify: `src/claude_manager/config.py`

- [x] **Step 1: Удалить константу `DEFAULT_PROJECTS_ROOT`**

В `src/claude_manager/config.py`, строки 22-24, удалить блок:

```python
# Значение PROJECTS_ROOT_DIR по умолчанию — папка, где у пользователя лежат проекты.
# Используется, если переменная окружения не задана. Бот ищет проекты здесь для команды /projects.
DEFAULT_PROJECTS_ROOT = "/Users/ivan/Desktop/claude-sandbox"
```

- [x] **Step 2: Переписать функцию `_resolve_projects_root`**

В том же файле найти функцию `_resolve_projects_root` (строки 94-107) и заменить целиком:

```python
def _resolve_projects_root(raw_value: str | None) -> str:
    """Определяет абсолютный путь к корневой папке со всеми проектами."""
    # Пустое значение — ошибка: PROJECTS_ROOT_DIR обязателен.
    if not raw_value:
        raise ConfigError(
            f"{_ENV_PROJECTS_ROOT} не задан. "
            "Укажите путь к корневой папке проектов в файле .env"
        )

    resolved_path = os.path.abspath(raw_value)

    if not os.path.isdir(resolved_path):
        raise ConfigError(
            f"{_ENV_PROJECTS_ROOT} указывает на несуществующую директорию: "
            f"'{resolved_path}'"
        )

    return resolved_path
```

### Task 3: Удалить устаревшие тесты на default-путь и проверить новые

**Files:**
- Modify: `tests/test_config.py`

- [x] **Step 1: Удалить импорт `DEFAULT_PROJECTS_ROOT` и устаревшие тесты**

В `tests/test_config.py`:

1. Строка 11 — убрать `DEFAULT_PROJECTS_ROOT,` из импорта `from claude_manager.config import (...)`.

2. Найти и удалить методы, которые проверяли fallback на default:
   - `TestResolveProjectsRoot.test_none_returns_default` (строки 266-271)
   - `TestResolveProjectsRoot.test_empty_returns_default` (строки 273-277)
   - `TestLoadConfigProjectsRoot.test_projects_root_default_when_missing` (строки 297-305)

3. В новых тестах из Task 1 (`test_none_raises_error`, `test_empty_raises_error`) убедиться, что они уже на месте.

- [x] **Step 2: Запустить все тесты config — должны проходить**

Run:
```bash
python -m pytest tests/test_config.py -v
```

Expected: все тесты `test_config.py` PASS (с новыми и без удалённых).

- [x] **Step 3: Закоммитить правки config**

> Закоммичено как `6594145 refactor(config): сделать PROJECTS_ROOT_DIR обязательным, удалить macOS-default` (+ доп. фикс `396052b fix(test_config): дефолтный PROJECTS_ROOT_DIR в _make_env для тестов`).

Run:
```bash
git add src/claude_manager/config.py tests/test_config.py
git commit -m "$(cat <<'EOF'
refactor(config): сделать PROJECTS_ROOT_DIR обязательным, удалить macOS-default

PROJECTS_ROOT_DIR теперь обязательная переменная окружения. Пустое или
отсутствующее значение даёт ConfigError. Удалена константа
DEFAULT_PROJECTS_ROOT, которая хардкодила macOS-путь.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Фаза 2. `src/claude_manager/main.py` — путь к логам

### Task 4: Заменить MAIN_LOG_PATH на XDG state directory

**Files:**
- Modify: `src/claude_manager/main.py`

- [x] **Step 1: Заменить MAIN_LOG_PATH и его docstring**

В `src/claude_manager/main.py`, строки 33-38, заменить блок:

```python
# Путь к основному лог-файлу — туда пишутся строки уровня INFO и выше.
# Совпадает с путём, куда LaunchAgent перенаправляет stderr, чтобы ротация
# применялась к тому же файлу.
MAIN_LOG_PATH = os.path.join(
    os.path.expanduser("~"), "Library", "Logs", "claude-manager.log"
)
```

На:

```python
# Путь к основному лог-файлу — туда пишутся строки уровня INFO и выше.
# XDG-стандартное место для пользовательского состояния: ~/.local/state/<app>/.
# Папка создаётся при первом запуске через os.makedirs(exist_ok=True).
MAIN_LOG_PATH = os.path.join(
    os.path.expanduser("~"), ".local", "state", "claude-manager", "claude-manager.log"
)
```

- [x] **Step 2: Запустить test_main.py**

Run:
```bash
python -m pytest tests/test_main.py -v
```

Expected: PASS. Тесты `test_main.py` не зависят от точного пути логов, проверяют только уровни логирования и фильтры — это разовая проверка, что мы ничего не сломали.

- [x] **Step 3: Закоммитить**

> Закоммичено как `ab08cbc refactor(main): перенести лог в XDG-стандартный путь ~/.local/state/`.

Run:
```bash
git add src/claude_manager/main.py
git commit -m "$(cat <<'EOF'
refactor(main): перенести лог в XDG-стандартный путь ~/.local/state/

Заменён macOS-специфичный ~/Library/Logs/ на XDG-стандартное место для
пользовательского состояния — ~/.local/state/claude-manager/. Совпадает
с практикой systemd user services на Linux.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Фаза 3. `src/claude_manager/bot.py` — обработчик `/restart`

### Task 5: Падающий тест на `handle_restart` через systemctl

**Files:**
- Modify: `tests/test_bot.py`

- [x] **Step 1: Найти место в `tests/test_bot.py` для тестов restart**

> Класс `TestHandleRestart` добавлен в конец файла. Импорт `handle_restart` добавлен в общий import-блок. Внутри файла уже существует `config_module` (alias для `from claude_manager import config as config_module`) — используется вместо плановой строки `from claude_manager import config`. Содержательно сигнатуры assert-ов совпадают со спекой.

Открыть `tests/test_bot.py`, дойти до конца файла (или найти существующий класс с другими обработчиками). Добавить новый класс в конец файла:

```python
class TestHandleRestart:
    """Тесты обработчика /restart — самоперезапуск через systemctl."""

    async def test_sends_warning_message_and_writes_marker(
        self, tmp_path: pytest.TempPathFactory, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        """handle_restart: пишет маркер с chat_id и шлёт «Перезапускаюсь через 2 сек»."""
        from claude_manager import bot

        marker_path = tmp_path / "restart-marker"
        monkeypatch.setattr(bot, "RESTART_MARKER_PATH", marker_path)

        update = MagicMock()
        update.effective_chat.id = 12345
        update.effective_user.id = next(iter(config.ALLOWED_USER_IDS))
        context = MagicMock()
        context.bot.send_message = AsyncMock()

        with patch("claude_manager.bot.asyncio.create_subprocess_exec", new=AsyncMock()) as mock_exec:
            await bot.handle_restart(update, context)

        assert marker_path.read_text() == "12345"
        context.bot.send_message.assert_awaited_once()
        sent_text = context.bot.send_message.call_args[0][1]
        assert "Перезапускаюсь" in sent_text
        assert "2" in sent_text

    async def test_launches_detached_systemctl_subprocess(
        self, tmp_path: pytest.TempPathFactory, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        """handle_restart: запускает bash -c 'sleep 2 && systemctl --user restart ...' detached."""
        from claude_manager import bot

        monkeypatch.setattr(bot, "RESTART_MARKER_PATH", tmp_path / "marker")

        update = MagicMock()
        update.effective_chat.id = 12345
        update.effective_user.id = next(iter(config.ALLOWED_USER_IDS))
        context = MagicMock()
        context.bot.send_message = AsyncMock()

        with patch("claude_manager.bot.asyncio.create_subprocess_exec", new=AsyncMock()) as mock_exec:
            await bot.handle_restart(update, context)

        mock_exec.assert_awaited_once()
        args, kwargs = mock_exec.call_args
        # bash, "-c", "sleep 2 && systemctl --user restart claude-manager.service"
        assert args[0] == "bash"
        assert args[1] == "-c"
        assert "systemctl --user restart claude-manager.service" in args[2]
        assert "sleep 2" in args[2]
        assert kwargs.get("start_new_session") is True
        # stdout/stderr должны быть DEVNULL чтобы пайпы не висели
        assert kwargs.get("stdout") == asyncio.subprocess.DEVNULL
        assert kwargs.get("stderr") == asyncio.subprocess.DEVNULL
```

Если в файле нет нужных импортов в начале — добавить:

```python
import asyncio
from unittest.mock import AsyncMock, MagicMock, patch
import pytest
from claude_manager import config
```

(проверить, что они уже есть — обычно есть).

- [~] **Step 2: Запустить новые тесты — должны падать**

Run:
```bash
python -m pytest tests/test_bot.py::TestHandleRestart -v
```

Expected: FAIL. Текущая реализация шлёт launchctl-команду, а не systemctl, и сообщение про «через 2 сек» содержит ссылку на kickstart.

> **Fail-first шаг фактически пропущен.** Тесты добавлены вместе с правками `bot.py` в одном
> diff (правки не закоммичены). Это значит, что мы не наблюдали «красную» фазу — тесты могут
> быть подогнаны под рабочий код, а не наоборот. Чтобы ретроспективно убедиться, что они
> «настоящие», можно временно откатить только правку `handle_restart` и убедиться, что тесты
> падают. На корректность результата это не влияет — оба теста сейчас зелёные и проверяют
> ровно то, что описано в спеке (см. Task 6 Step 3).

### Task 6: Переписать `handle_restart` и переименовать константы

**Files:**
- Modify: `src/claude_manager/bot.py`

- [x] **Step 1: Удалить LAUNCHD_SERVICE_LABEL, переименовать константу задержки**

> Сделано в `bot.py` (не закоммичено). `LAUNCHD_SERVICE_LABEL` удалён, `RESTART_DELAY_BEFORE_KICKSTART_SECONDS` → `RESTART_DELAY_BEFORE_SYSTEMCTL_SECONDS`, комментарий `RESTART_MARKER_PATH` обновлён ровно по плану.

В `src/claude_manager/bot.py`, строки 107-114, заменить блок:

```python
# Метка launchd-сервиса бота (используется для самоперезапуска через /restart)
LAUNCHD_SERVICE_LABEL = "com.ivan.claude-manager"

# Задержка перед kickstart — чтобы бот успел ответить пользователю до своей смерти
RESTART_DELAY_BEFORE_KICKSTART_SECONDS = 2

# Маркер-файл для отправки подтверждения после перезапуска через /restart
RESTART_MARKER_PATH = Path("/tmp/claude-manager-restart-chat-id")
```

На:

```python
# Задержка перед systemctl restart — чтобы бот успел ответить пользователю
# до того, как systemd пришлёт ему SIGTERM.
RESTART_DELAY_BEFORE_SYSTEMCTL_SECONDS = 2

# Маркер-файл для отправки подтверждения после перезапуска через /restart.
# Новый процесс читает chat_id из этого файла в post_init и шлёт «готов».
RESTART_MARKER_PATH = Path("/tmp/claude-manager-restart-chat-id")
```

- [x] **Step 2: Переписать функцию `handle_restart`**

> Сделано в `bot.py` (не закоммичено). Docstring обновлён («через systemd»), команда заменена на `systemctl --user restart claude-manager.service`, структура (send_message → write_text → create_subprocess_exec с DEVNULL и `start_new_session=True`) сохранена. Inline-комментарии переписаны под `systemctl restart` + cgroup.
>
> **Замечание (не блокер):** после удаления `os.getuid()` модуль `os` в `bot.py` больше нигде не используется, но `import os` на строке 10 остался. Это мёртвый импорт — желательно убрать в этом же коммите, чтобы не плодить шум.

Найти `handle_restart` (строки 1290-1322) и заменить целиком:

```python
async def handle_restart(
    update: Update, context: ContextTypes.DEFAULT_TYPE
) -> None:
    """Обработчик команды /restart — самоперезапуск бота через systemd.

    Запускает отвязанный subprocess, который через задержку выполняет
    systemctl --user restart. Задержка нужна, чтобы бот успел отправить
    подтверждение пользователю до того, как systemd пришлёт ему SIGTERM.
    """
    if not _check_access(update):
        return

    chat_id = update.effective_chat.id
    await context.bot.send_message(
        chat_id,
        f"Перезапускаюсь через {RESTART_DELAY_BEFORE_SYSTEMCTL_SECONDS} сек...",
    )
    RESTART_MARKER_PATH.write_text(str(chat_id))
    logger.info("Запущен самоперезапуск через /restart")

    # Отвязанный процесс (start_new_session=True) — чтобы systemctl restart
    # пережил смерть бота-инициатора. Без отвязки systemd убил бы subprocess
    # как часть cgroup сервиса claude-manager.service.
    # DEVNULL для stdout/stderr — чтобы не держать пайпы открытыми.
    restart_command = (
        f"sleep {RESTART_DELAY_BEFORE_SYSTEMCTL_SECONDS} && "
        "systemctl --user restart claude-manager.service"
    )
    await asyncio.create_subprocess_exec(
        "bash", "-c", restart_command,
        start_new_session=True,
        stdout=asyncio.subprocess.DEVNULL,
        stderr=asyncio.subprocess.DEVNULL,
    )
```

- [x] **Step 3: Запустить тесты restart и убедиться, что они проходят**

Run:
```bash
python -m pytest tests/test_bot.py::TestHandleRestart -v
```

Expected: PASS оба теста.

> Подтверждено: `2 passed in 0.26s`. Полный `tests/test_bot.py` — `102 passed in 0.73s`.

- [x] **Step 4: Прогнать все Python-тесты, чтобы поймать косвенные падения**

Run:
```bash
python -m pytest tests/ --ignore=tests/e2e -v 2>&1 | tail -30
```

Expected: все тесты PASS, кроме `tests/test_restart_claude_manager_script.py` (он переписывается отдельно в Фазе 4 — там падения ожидаемы).

Если падает что-то ещё — разобрать падение **до** перехода к следующей задаче. Тесты, ссылающиеся на удалённые константы (например, `LAUNCHD_SERVICE_LABEL`), нужно поправить точечно.

> Прогнано: `1009 passed, 6 skipped, 1 failed in 90.07s`. Единственное падение —
> `test_session_lifecycle.py::TestFilePersistence::test_registry_survives_reload` —
> это унаследованный baseline-failure (см. Task 0), не связан с правками `bot.py`.
> Все 102 теста `test_bot.py` зелёные, включая 2 новых из `TestHandleRestart`.
> Не вижу теста, который бы упал из-за удаления `LAUNCHD_SERVICE_LABEL` — значит,
> на эту константу извне никто не ссылался.

- [x] **Step 5: Закоммитить правки bot.py**

> Закоммичено как `fb4846e feat(bot): /restart через systemctl вместо launchctl kickstart`.
> Дополнительно убран мёртвый `import os` из `bot.py` (после удаления `os.getuid()` модуль
> больше нигде не использовался) — упомянуто в commit body.

Run:
```bash
git add src/claude_manager/bot.py tests/test_bot.py
git commit -m "$(cat <<'EOF'
feat(bot): /restart через systemctl вместо launchctl kickstart

handle_restart теперь запускает отвязанный subprocess с
'sleep 2 && systemctl --user restart claude-manager.service'.
Удалена константа LAUNCHD_SERVICE_LABEL, RESTART_DELAY_BEFORE_KICKSTART
переименована в RESTART_DELAY_BEFORE_SYSTEMCTL. Добавлен класс
тестов TestHandleRestart с проверкой содержимого команды и detach-флагов.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Фаза 4. Shell-скрипты

### Task 7: Удалить `start-claude-manager.sh`

**Files:**
- Delete: `start-claude-manager.sh`
- Delete: `tests/test_start_claude_manager_script.py` (находка — план её пропустил)

- [x] **Step 1: Удалить файл**

> Удалено через `git rm start-claude-manager.sh`. **Дополнительно удалён**
> `tests/test_start_claude_manager_script.py` — он проверял конкретные строки
> в удалённой macOS-обёртке (`trap terminate_running_bot_process_tree TERM INT`,
> `pgrep -P "$parent_process_id"` и т.п.). На Linux эту роль играет systemd
> через `KillMode=control-group` по умолчанию, обёртки и тестировать нечего.
> Удаление обнаружено через полный `pytest` после Tasks 7-10 — этот тест начал
> падать с `FileNotFoundError`, потому что ссылался на удалённый скрипт.

### Task 8: Переписать `restart-claude-manager.sh` под Linux

**Files:**
- Rewrite: `restart-claude-manager.sh`

- [ ] **Step 1: Заменить содержимое файла**

Открыть `restart-claude-manager.sh` и полностью заменить содержимое на:

```bash
#!/bin/bash
set -e

# Безопасный рестарт claude-manager через systemd.
# Выполняет preflight (editable install здоров?), systemctl restart, post-flight
# (сервис активен + Python-бот запущен?). Exit 0 — успех, exit 1 — провал с диагностикой.
# ВАЖНО: только для ВНЕШНЕГО вызова (терминал). Самоперезапуск из бота — /restart.

SERVICE_NAME="claude-manager.service"
PROJECT_DIR="$(cd "$(dirname "$0")" && pwd -P)"
VENV_BIN="${PROJECT_DIR}/.venv/bin"
LOG_FILE="$HOME/.local/state/claude-manager/claude-manager.log"

POST_FLIGHT_CHECK_COUNT=3
POST_FLIGHT_CHECK_INTERVAL_SECONDS=5
LOG_TAIL_LINES=30

# --- Helpers (тестируются отдельно через source-only mode) ---

check_editable_install() {
    local venv_bin="${1:-$VENV_BIN}"

    if [[ ! -x "${venv_bin}/claude-manager" ]]; then
        echo "FAIL: ${venv_bin}/claude-manager не найден или не исполняем"
        return 1
    fi
    if ! "${venv_bin}/python" -c "import claude_manager" 2>&1; then
        echo "FAIL: модуль claude_manager не импортируется"
        "${venv_bin}/python" -c "import sys; print('python:', sys.executable); print('version:', sys.version)" 2>&1
        return 1
    fi
    return 0
}

service_is_running() {
    local service="${1:-$SERVICE_NAME}"
    systemctl --user is-active --quiet "$service" \
        && pgrep -f "claude_manager" >/dev/null 2>&1
}

print_diagnostics_on_failure() {
    local service="${1:-$SERVICE_NAME}"
    local log_file="${2:-$LOG_FILE}"

    echo ""
    echo "=== Последние ${LOG_TAIL_LINES} строк ${log_file} ==="
    tail -"$LOG_TAIL_LINES" "$log_file" 2>/dev/null || echo "(лог не найден)"
    echo ""
    echo "=== Последние ${LOG_TAIL_LINES} строк journalctl ==="
    journalctl --user -u "$service" -n "$LOG_TAIL_LINES" --no-pager 2>/dev/null \
        || echo "(journal недоступен)"
}

# --- Source-only mode (для тестов: загружаем функции, не запускаем основной поток) ---

if [[ "${CLAUDE_MANAGER_RESTART_SOURCE_ONLY:-0}" == "1" ]]; then
    return 0 2>/dev/null || exit 0
fi

# --- Main flow ---

echo "=== Preflight ==="
if ! check_editable_install; then
    echo ""
    echo "Починка: cd $PROJECT_DIR && source .venv/bin/activate && pip install -e \".[dev]\""
    exit 1
fi
echo "OK: editable install здоров"

echo ""
echo "=== Restart ==="
systemctl --user restart "$SERVICE_NAME"
echo "systemctl --user restart отправлен"

echo ""
echo "=== Post-flight ==="
POST_FLIGHT_OK=false
for CHECK_NUMBER in $(seq 1 "$POST_FLIGHT_CHECK_COUNT"); do
    sleep "$POST_FLIGHT_CHECK_INTERVAL_SECONDS"

    if service_is_running; then
        SERVICE_PID=$(systemctl --user show -p MainPID --value "$SERVICE_NAME")
        echo "OK: $SERVICE_NAME active, MainPID=$SERVICE_PID (попытка $CHECK_NUMBER/$POST_FLIGHT_CHECK_COUNT)"
        POST_FLIGHT_OK=true
        break
    fi

    if [[ "$CHECK_NUMBER" -lt "$POST_FLIGHT_CHECK_COUNT" ]]; then
        echo "WARN: попытка $CHECK_NUMBER/$POST_FLIGHT_CHECK_COUNT — сервис ещё не активен"
    fi
done

if [[ "$POST_FLIGHT_OK" != "true" ]]; then
    echo "FAIL: сервис не поднялся за $POST_FLIGHT_CHECK_COUNT проверок"
    print_diagnostics_on_failure
    exit 1
fi

echo ""
echo "=== Последние строки лога ==="
tail -"$LOG_TAIL_LINES" "$LOG_FILE" 2>/dev/null || echo "(лог не найден)"

echo ""
echo "=== Рестарт завершён успешно ==="
```

- [x] **Step 2: Сделать исполняемым**

> Выполнено: `chmod +x restart-claude-manager.sh`, права `-rwxrwx--x`.

### Task 9: Переписать тесты shell-скрипта

**Files:**
- Rewrite: `tests/test_restart_claude_manager_script.py`

- [ ] **Step 1: Заменить содержимое теста**

Открыть `tests/test_restart_claude_manager_script.py` и заменить целиком:

```python
"""Tests for the Linux restart shell script."""

from pathlib import Path
import subprocess


PROJECT_ROOT = Path(__file__).resolve().parents[1]
RESTART_SCRIPT_PATH = PROJECT_ROOT / "restart-claude-manager.sh"


def _run_helper(helper_invocation: str) -> subprocess.CompletedProcess[str]:
    """Source the restart script in source-only mode and run a helper invocation."""
    bash_command = (
        "CLAUDE_MANAGER_RESTART_SOURCE_ONLY=1 "
        f"source {RESTART_SCRIPT_PATH}; "
        f"{helper_invocation}"
    )
    return subprocess.run(
        ["bash", "-c", bash_command],
        check=False,
        text=True,
        capture_output=True,
    )


# --- check_editable_install ---


def test_check_editable_install_fails_when_binary_missing(tmp_path: Path) -> None:
    """Нет .venv/bin/claude-manager — return 1 с понятным сообщением."""
    fake_bin = tmp_path / "empty-bin"
    fake_bin.mkdir()

    result = _run_helper(f'check_editable_install "{fake_bin}"')

    assert result.returncode == 1
    assert "не найден" in result.stdout


def test_check_editable_install_fails_when_import_breaks(tmp_path: Path) -> None:
    """Бинарник есть, но import claude_manager падает — return 1."""
    fake_bin = tmp_path / "broken-bin"
    fake_bin.mkdir()
    # Подкладываем фейковый claude-manager (чтобы прошёл первый check)
    fake_claude_manager = fake_bin / "claude-manager"
    fake_claude_manager.write_text("#!/bin/bash\nexit 0\n")
    fake_claude_manager.chmod(0o755)
    # И фейковый python, который падает при -c "import claude_manager"
    fake_python = fake_bin / "python"
    fake_python.write_text("#!/bin/bash\nexit 1\n")
    fake_python.chmod(0o755)

    result = _run_helper(f'check_editable_install "{fake_bin}"')

    assert result.returncode == 1
    assert "не импортируется" in result.stdout


# --- service_is_running ---


def test_service_is_running_returns_zero_when_both_pass() -> None:
    """systemctl is-active 0 + pgrep 0 → return 0."""
    result = _run_helper(
        'systemctl() { return 0; }; '
        'pgrep() { return 0; }; '
        'service_is_running'
    )
    assert result.returncode == 0


def test_service_is_running_returns_nonzero_when_systemctl_inactive() -> None:
    """systemctl is-active не-0 → return 1, даже если pgrep успешен."""
    result = _run_helper(
        'systemctl() { return 3; }; '
        'pgrep() { return 0; }; '
        'service_is_running'
    )
    assert result.returncode != 0


def test_service_is_running_returns_nonzero_when_pgrep_misses() -> None:
    """systemctl is-active 0, но pgrep не нашёл процесс → return 1."""
    result = _run_helper(
        'systemctl() { return 0; }; '
        'pgrep() { return 1; }; '
        'service_is_running'
    )
    assert result.returncode != 0


# --- print_diagnostics_on_failure ---


def test_print_diagnostics_prints_log_and_journal_sections(tmp_path: Path) -> None:
    """Диагностика печатает обе секции, даже если файлов/journal нет."""
    nonexistent_log = tmp_path / "no-such-log.log"

    result = _run_helper(
        'journalctl() { echo "FAKE_JOURNAL_LINE"; }; '
        f'print_diagnostics_on_failure "fake.service" "{nonexistent_log}"'
    )

    assert result.returncode == 0
    assert "строк" in result.stdout  # заголовки секций
    assert "лог не найден" in result.stdout  # fallback при отсутствии файла
    assert "FAKE_JOURNAL_LINE" in result.stdout
```

- [x] **Step 2: Запустить тесты shell-скрипта**

> Прогнано: `6 passed in 0.07s` — все новые тесты `check_editable_install`,
> `service_is_running`, `print_diagnostics_on_failure` зелёные.

### Task 10: Переписать `watch_and_restart.sh` под Linux

**Files:**
- Rewrite: `watch_and_restart.sh`

- [ ] **Step 1: Заменить содержимое файла**

Заменить целиком:

```bash
#!/bin/bash
set -e

# Watcher: при изменении .py файлов в src/ перезапускает бота через systemctl.
# Зависимость: inotify-tools (sudo apt install inotify-tools).
# Запуск: ./watch_and_restart.sh
# Остановка: Ctrl+C

SERVICE_NAME="claude-manager.service"
PROJECT_DIR="$(cd "$(dirname "$0")" && pwd -P)"
SRC_DIR="$PROJECT_DIR/src"
DEBOUNCE_SECONDS=1

# --- Цвета для читаемости ---
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
RESET='\033[0m'

log_info()  { echo -e "${GREEN}[INFO]${RESET}  $(date '+%H:%M:%S') $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${RESET}  $(date '+%H:%M:%S') $1"; }
log_error() { echo -e "${RED}[ERROR]${RESET} $(date '+%H:%M:%S') $1"; }

# --- Проверка зависимости ---
if ! command -v inotifywait >/dev/null 2>&1; then
    log_error "Не найдена команда inotifywait."
    echo "  Установите пакет inotify-tools:"
    echo "    sudo apt install inotify-tools     # Debian/Ubuntu"
    echo "    sudo dnf install inotify-tools     # Fedora/RHEL"
    exit 1
fi

log_info "Наблюдатель запущен. Слежу за: $SRC_DIR"
log_info "Сервис: $SERVICE_NAME (Ctrl+C для остановки)"
echo ""

last_restart_timestamp=0

# inotifywait -m печатает события по одному на строку.
# -r — рекурсивно, --include — фильтр regex по полному пути.
# Используем process substitution, чтобы переменная last_restart_timestamp
# жила между итерациями цикла (pipe создаёт subshell).
while read -r event_line; do
    now=$(date +%s)
    since_last=$((now - last_restart_timestamp))

    # Дебаунс: если прошло меньше DEBOUNCE_SECONDS — пропускаем
    if [[ "$since_last" -lt "$DEBOUNCE_SECONDS" ]]; then
        continue
    fi

    log_warn "Изменения: $event_line"
    log_info "Перезапускаю $SERVICE_NAME..."

    if systemctl --user restart "$SERVICE_NAME"; then
        log_info "Сервис перезапущен"
    else
        log_error "Перезапуск $SERVICE_NAME провалился (exit $?)"
    fi

    last_restart_timestamp=$(date +%s)
done < <(inotifywait -m -r -e modify,create,delete --include '.*\.py$' "$SRC_DIR" --format '%w%f %e' 2>/dev/null)
```

- [x] **Step 2: Сделать исполняемым**

> Выполнено: `chmod +x watch_and_restart.sh`, права `-rwxrwx--x`.

- [x] **Step 3: Прогнать все Python-тесты, чтобы проверить полную зелень**

> Прогнано: `1010 passed, 6 skipped, 1 failed in 91.17s`. Единственное падение —
> унаследованный baseline-failure `test_registry_survives_reload` (см. Task 0).
> Изменение числа passed: было 1009 (с 4 macOS-shell-тестами), стало 1010
> (с 6 новыми Linux-shell-тестами − 1 удалённый `test_start_claude_manager_script.py`),
> разница `+6−4−1 = +1` сходится. Регрессий нет.

- [x] **Step 4: Закоммитить shell-скрипты и их тесты**

> Закоммичено как `8ebe4cd chore(scripts): переписать restart/watch-скрипты под Linux + systemd`.
> 5 files changed, 175 insertions, 446 deletions.

Run:
```bash
git add restart-claude-manager.sh watch_and_restart.sh tests/test_restart_claude_manager_script.py
git rm --cached start-claude-manager.sh 2>/dev/null || true
git commit -m "$(cat <<'EOF'
chore(scripts): переписать restart/watch-скрипты под Linux + systemd

- restart-claude-manager.sh: systemctl --user restart вместо launchctl
  kickstart, post-flight через is-active + pgrep, диагностика через
  journalctl + tail XDG-лога. Хелперы вынесены для тестируемости.
- watch_and_restart.sh: inotifywait вместо file fingerprint polling,
  systemctl restart вместо самописного kill + start, дебаунс 1 сек.
- start-claude-manager.sh удалён — на Linux systemd сам делает retry
  через Restart=always + RestartSec=10.
- Тесты shell-скрипта переписаны под новые хелперы.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Фаза 5. Документация

> **Дополнительная находка для Phase 5 (план не предусмотрел).** В `src/claude_manager/main.py`
> остались два комментария с упоминанием LaunchAgent:
>
> - Строка 21: `# независимо от рабочей директории (LaunchAgent, watch_and_restart.sh, ручной запуск)`
> - Строки 55-58: `# StreamHandler пишет в stderr. Под LaunchAgent stderr перенаправлен в error.log...`
>
> Это **только комментарии**, не код — `LOCK_PATH` уже в `~/`, а StreamHandler-логика
> по-прежнему корректна (просто описание устарело). Поправить во время Phase 5 одним
> Edit-ом — заменить `LaunchAgent` → `systemd` и обновить пояснение про двойное логирование
> (на Linux stderr → journalctl, RotatingFileHandler → отдельный файл, дубликата нет).

### Task 11: Чистка `CLAUDE.md`

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Удалить раздел «Принципы эксплуатации» — три macOS-only буллета**

В `CLAUDE.md`, в разделе «Принципы эксплуатации» (строки 153-158), удалить целиком три буллета:

- «Принцип изоляции venv от TCC-защищённых директорий» (буллет на строке 156)
- «Принцип изоляции скрипта запуска от TCC-зоны» (буллет на строке 157)
- «Принцип запрета самоперезапуска из собственного дерева процессов» — оставить, он не привязан к OS.

После правки в разделе должны остаться: «verify-before-and-after» (с переписыванием — Step 2) и «запрет самоперезапуска».

- [ ] **Step 2: Переписать «Принцип verify-before-and-after для рестарта сервисов»**

Заменить буллет (строка 155) на:

```markdown
- **Принцип verify-before-and-after для рестарта сервисов.** Любой рестарт через `systemctl --user restart` или аналог обязан включать pre-flight (проверка editable install, import, конфиг, зависимости) и post-flight. Post-flight обязан проверять не только активность сервиса (`systemctl --user is-active`), но и наличие запущенного Python-процесса (`pgrep -f claude_manager`) — между этими двумя сигналами может быть зазор, в течение которого systemd считает сервис активным, а Python-бот ещё инициализируется. Stale exit codes от systemd обрабатываются через `systemctl reset-failed claude-manager.service` при необходимости. Голый `systemctl restart` без проверок — запрещён. Для claude-manager использовать `restart-claude-manager.sh` (preflight + restart + post-flight). Инцидент-источник: 20-04-2026, рестарт без pre-flight после пересоздания venv — бот работал на инерции живого процесса, рестарт обнажил сломанный editable install.
```

- [ ] **Step 3: Удалить «Принцип retry-обёртки для launchd-сервисов»**

Удалить целиком блок (строки 204-206):

```markdown
## Принцип retry-обёртки для launchd-сервисов

Python-сервисы под launchd запускаются через shell-обёртку ... [весь параграф]
```

На Linux systemd сам делает retry через `Restart=always` + `RestartSec=10` — обёртка не нужна.

- [ ] **Step 4: Обновить «Команды разработки»**

В разделе «Команды разработки» (строки 208-242):

Заменить блок:
```markdown
# Автозапуск через macOS LaunchAgents (бот стартует при входе в систему)
launchctl load ~/Library/LaunchAgents/com.ivan.claude-manager.plist
launchctl unload ~/Library/LaunchAgents/com.ivan.claude-manager.plist  # остановить
```

На:
```markdown
# Управление сервисом systemd (бот стартует автоматически после enable)
systemctl --user enable claude-manager.service   # включить автозапуск
systemctl --user start claude-manager.service    # запустить
systemctl --user stop claude-manager.service     # остановить
systemctl --user disable claude-manager.service  # убрать из автозапуска
systemctl --user status claude-manager.service   # статус
```

Заменить блок:
```markdown
# Рестарт бота (preflight + kickstart + post-flight)
# ТОЛЬКО из терминала или внешнего агента. НЕ вызывать из подпроцесса бота — exit 137
./restart-claude-manager.sh

# Логи автозапуска
cat ~/Library/Logs/claude-manager.log
```

На:
```markdown
# Рестарт бота (preflight + systemctl restart + post-flight)
# ТОЛЬКО из терминала или внешнего агента. НЕ вызывать из подпроцесса бота — exit 137
./restart-claude-manager.sh

# Логи приложения (RotatingFileHandler)
tail -f ~/.local/state/claude-manager/claude-manager.log

# Логи сервиса (stdout/stderr под systemd)
journalctl --user -u claude-manager.service -f
```

- [ ] **Step 5: Обновить «Структура проекта»**

В разделе «Структура проекта» (строки 60-63 примерно), удалить строки:

```markdown
├── start-claude-manager.sh   # Обёртка для launchd (reference-копия, launchd использует ~/.local/bin/start-claude-manager.sh)
├── .venv -> ~/.venvs/claude-manager/  # Симлинк на venv вне TCC-зоны Desktop
```

Заменить строку (60) описание `restart-claude-manager.sh`:

Было: `# Рестарт бота: preflight + kickstart + post-flight (только из терминала)`
Стало: `# Рестарт бота: preflight + systemctl restart + post-flight (только из терминала)`

Также проверить строку «**ОС:** macOS (fcntl для файловой блокировки)» (строка 52). Заменить на:
```markdown
- **ОС:** Linux (systemd user services, fcntl для файловой блокировки доступен)
```

- [ ] **Step 6: Переписать «Принцип обязательного editable install»**

В разделе «Принцип обязательного editable install» (строки 200-202) заменить:

```markdown
## Принцип обязательного editable install

Editable install (`pip install -e .`) — обязательная часть рабочего venv. Venv живёт в `/home/ivan/claude-sandbox/claude_manager/.venv/`. После пересоздания venv: `python3.13 -m venv .venv && source .venv/bin/activate && pip install -e ".[dev]"` и проверить: `python -c "import claude_manager"`. Без editable install `claude-manager` (entry point из `pyproject.toml`) не появится в `.venv/bin/`, а `systemctl --user restart` упадёт с «No such file or directory». Также `python -m claude_manager` упадёт с `No module named claude_manager` — но **может не упасть сразу**, если живой процесс держит модуль в памяти. Мина сработает при первом рестарте. Venv без проверенного editable install считается сломанным.
```

- [ ] **Step 7: Проверить упоминания «macOS» / «EDEADLK» / «LaunchAgent» в остальном CLAUDE.md**

Run:
```bash
grep -n "macOS\|launchctl\|launchd\|LaunchAgent\|~/Library\|~/.venvs\|TCC\|EDEADLK\|UF_HIDDEN" CLAUDE.md
```

Прочитать каждое совпадение и решить точечно:
- Упоминания «macOS» в контексте платформы — заменить на «Linux» или удалить.
- `~/Library/Logs/...` → `~/.local/state/claude-manager/...`.
- `EDEADLK` — это macOS-специфика APFS, на Linux нерелевантно. Удалить буллет «Транзиентная ошибка EDEADLK (errno 11) на macOS» из «Важные детали для разработки» (строка 262).
- `~/.venvs/claude-manager` в «Переключение проектов» (строка 258 — `по умолчанию /Users/ivan/Desktop/claude-sandbox`) — этот default тоже macOS. Заменить упоминание умолчания на «задаётся переменной окружения `PROJECTS_ROOT_DIR` (обязательная переменная, без default)».

### Task 12: Полностью переписать `dev/docs/bot-launch-infrastructure.md`

**Files:**
- Rewrite: `dev/docs/bot-launch-infrastructure.md`

- [ ] **Step 1: Заменить содержимое файла**

Открыть `dev/docs/bot-launch-infrastructure.md` и заменить целиком:

```markdown
# Инфраструктура запуска бота

Справочник по всем компонентам, которые участвуют в запуске и работе Claude Manager на Linux. Описывает где каждый компонент лежит, зачем он там, как компоненты связаны между собой и как их чинить.

## Где живёт код и состояние

Проект лежит в `/home/ivan/claude-sandbox/claude_manager/`. Venv — в `.venv/` внутри проекта (на Linux нет macOS-овской TCC-защищённой Desktop-зоны, поэтому venv можно держать рядом с кодом без проблем с editable install).

### В проекте

- **`src/claude_manager/`** — продакшен-код бота.
- **`.env`** — секреты (токен, ID пользователей) — в `.gitignore`.
- **`.venv/`** — виртуальное окружение Python с editable install (`pip install -e ".[dev]"`).
- **`restart-claude-manager.sh`** — скрипт безопасного рестарта (preflight + systemctl + post-flight).
- **`watch_and_restart.sh`** — наблюдатель для разработки (inotifywait + systemctl restart).
- **`sessions.json`, `daily_sessions.json`** — файлы состояния (привязки чат ↔ сессия, дневные номера).

### Вне проекта

- **`~/.config/systemd/user/claude-manager.service`** — unit-файл systemd для автозапуска.
- **`~/.local/state/claude-manager/claude-manager.log`** — основной лог (RotatingFileHandler, XDG-стандарт).
- **`~/.claude-manager.lock`** — файловая блокировка от двойного запуска (fcntl.flock).
- **`~/.claude-manager-current-project`** — последний выбранный проект (восстанавливается при старте).
- **`~/.claude-manager-silence-mode`** — состояние silence mode (persisted между перезапусками).
- **`~/.claude-manager-current-backend`** — выбранный CLI-бэкенд для новых сессий.
- **journalctl** — stdout/stderr бота под systemd попадают в `journalctl --user -u claude-manager.service`.

## systemd user service

Файл: `~/.config/systemd/user/claude-manager.service`.

Текущее содержимое:

```ini
[Unit]
Description=Claude Manager Telegram bot
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/ivan/claude-sandbox/claude_manager
Environment=PATH=/home/ivan/.npm-global/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=/home/ivan/claude-sandbox/claude_manager/.venv/bin/claude-manager
Restart=always
RestartSec=10
TimeoutStopSec=30

[Install]
WantedBy=default.target
```

Ключевые поля:

- **`ExecStart`** — entry point `claude-manager`, поставляемый pip-овым editable install из `pyproject.toml`. На прямой запуск Python через `python -m` не переходим — entry point чище и не требует костылей с `sys.path`.
- **`Restart=always`** + **`RestartSec=10`** — systemd сам перезапускает бота при падении через 10 секунд. Раньше эту роль выполняла shell-обёртка с retry-логикой; на Linux она не нужна.
- **`TimeoutStopSec=30`** — даёт боту до 30 секунд на корректное завершение после SIGTERM (важно для самоперезапуска через `/restart`).
- **`Type=simple`** — stdout/stderr автоматически попадают в `journalctl`.
- **`Environment=PATH=...`** — обязательно включить путь к `node` для Claude CLI (это Node-приложение). Без этого Claude CLI падает с `env: node: No such file or directory`.

## Цепочка запуска

```
systemd (user)
  → ExecStart=/home/ivan/claude-sandbox/claude_manager/.venv/bin/claude-manager
    → claude_manager.main:main() (entry point из pyproject.toml)
      → файловая блокировка через fcntl
      → загрузка config, восстановление сессий
      → Telegram polling
```

## Самоперезапуск через `/restart`

Команда `/restart` в Telegram запускает отвязанный subprocess:

```
бот → asyncio.create_subprocess_exec("bash", "-c",
        "sleep 2 && systemctl --user restart claude-manager.service",
        start_new_session=True, stdout=DEVNULL, stderr=DEVNULL)
```

Через 2 секунды subprocess (отвязанный от cgroup сервиса через `start_new_session=True`) выполняет `systemctl --user restart`. systemd шлёт SIGTERM текущему процессу бота, ждёт до 30 секунд (`TimeoutStopSec=30`), запускает новый процесс через `ExecStart`. В `post_init` нового процесса бот читает маркер-файл `/tmp/claude-manager-restart-chat-id`, шлёт пользователю «Перезапустился, готов к работе», удаляет маркер.

## Внешний рестарт: `restart-claude-manager.sh`

Скрипт в корне проекта. Безопасен для запуска из терминала или внешнего агента, **не** из подпроцесса самого бота (бот убьёт собственное дерево — exit 137).

Поток:

1. **Preflight** — `check_editable_install` проверяет наличие `.venv/bin/claude-manager` и успешность `import claude_manager`. При ошибке — exit 1 + подсказка по починке.
2. **Restart** — `systemctl --user restart claude-manager.service`.
3. **Post-flight** — 3 попытки с интервалом 5 секунд. Успех = `systemctl --user is-active` + `pgrep -f claude_manager`. При провале — диагностика через `tail` лога и `journalctl`, exit 1.

Хелперы (`check_editable_install`, `service_is_running`, `print_diagnostics_on_failure`) вынесены в отдельные функции и покрыты юнит-тестами в `tests/test_restart_claude_manager_script.py`.

## Watcher для разработки: `watch_and_restart.sh`

Использует `inotifywait` из пакета `inotify-tools`. При изменении любого `.py`-файла в `src/` шлёт `systemctl --user restart claude-manager.service`. Дебаунс 1 секунда защищает от шквала событий при сохранении нескольких файлов одновременно в IDE.

Зависимость: `sudo apt install inotify-tools` (Debian/Ubuntu) или `sudo dnf install inotify-tools` (Fedora). Скрипт проверяет наличие команды на старте.

## Пересоздание venv

```bash
cd /home/ivan/claude-sandbox/claude_manager
rm -rf .venv
python3.13 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"

# Проверка: entry point на месте, импорт проходит
ls .venv/bin/claude-manager
python -c "import claude_manager; print('import OK')"
```

После пересоздания обязательно прогнать `./restart-claude-manager.sh` для проверки. Бот может работать «по инерции» (живой процесс держит модуль в памяти), но при первом рестарте упадёт с `No module named claude_manager`, если editable install не прошёл.

## Диагностика

### Бот не запускается

```bash
# 1. Статус сервиса
systemctl --user status claude-manager.service

# 2. Последние строки journalctl (stdout/stderr под systemd)
journalctl --user -u claude-manager.service -n 50 --no-pager

# 3. Лог приложения (RotatingFileHandler)
tail -50 ~/.local/state/claude-manager/claude-manager.log

# 4. Проверить editable install
source .venv/bin/activate
python -c "import claude_manager; print('OK')"

# 5. Проверить, что entry point на месте
ls -l .venv/bin/claude-manager
```

### Сервис в состоянии `failed`

systemd считает сервис «провалившимся», когда `Restart=always` исчерпал retry-budget. Сбросить статус:

```bash
systemctl --user reset-failed claude-manager.service
systemctl --user start claude-manager.service
```

### `claude-manager: command not found` в journalctl

Editable install сломан — entry point из `pyproject.toml` не зарегистрирован. Пересоздать venv (см. выше).

### Бот запущен в двух экземплярах

Защита: файловая блокировка `~/.claude-manager.lock` через `fcntl.flock`. Вторая копия (например, ручной `python -m claude_manager` параллельно с systemd-сервисом) сразу завершится с понятным сообщением.

## Связанные документы

- `deployment-guide.md` — пошаговая установка от нуля.
- `dev/docs/specs/28.05_17.25-linux-bot-commands-cleanup-spec.md` — спека миграции с macOS на Linux.
- `CLAUDE.md`, секции «Принципы эксплуатации» и «Команды разработки».
```

### Task 13: Полностью переписать `dev/docs/deployment-guide.md`

**Files:**
- Rewrite: `dev/docs/deployment-guide.md`

- [ ] **Step 1: Заменить содержимое файла**

Открыть `dev/docs/deployment-guide.md` и заменить целиком:

```markdown
# Гайд по запуску Claude Manager

Пошаговая инструкция: от чистой Linux-системы до работающего бота.

## Что нужно заранее

- **Linux** с systemd (Ubuntu 22+, Debian 12+, Fedora 38+, Arch — стандартные дистрибутивы). Бот использует `systemd --user` для автозапуска и `inotify-tools` для watcher разработки.
- **Python 3.13** — `python3.13 --version`. Если в системе нет — установить через `apt install python3.13` или `pyenv`.
- **Claude Code CLI** — `claude --version` должна работать в терминале. Бот общается с этим CLI через stream-json.
- **Telegram-бот** — создать через @BotFather в Telegram, получить токен.

## Шаг 1: Установка

```bash
# Клонировать репозиторий
git clone <url-репозитория>
cd claude_manager

# Создать виртуальное окружение
python3.13 -m venv .venv

# Активировать
source .venv/bin/activate

# Установить зависимости + editable install (entry point claude-manager)
pip install -e ".[dev]"

# Проверить, что модуль импортируется и entry point на месте
python -c "import claude_manager; print('OK')"
ls .venv/bin/claude-manager
```

**Ожидаемый результат:** `import OK` и видим путь `.venv/bin/claude-manager`.

**Важно:** `pip install -e .` обязателен — без editable install systemd не сможет запустить бота (`claude-manager` отсутствует в `.venv/bin/`).

## Шаг 2: Настройка `.env`

```bash
cp .env.example .env
```

Открыть `.env` в редакторе, заполнить:

- **TELEGRAM_BOT_TOKEN** — токен от @BotFather (длинная строка вида `123456:ABC-DEF1234...`).
- **ALLOWED_USER_IDS** — ваш Telegram-ID (узнать через @userinfobot). Несколько ID через запятую, но только для одного человека с разных устройств.
- **PROJECTS_ROOT_DIR** — корневая папка со всеми проектами для команды `/projects`. Обязательная переменная.

Необязательное:

- **CLAUDE_WORKING_DIR** — рабочая папка по умолчанию для Claude. Без указания — текущая папка.
- **E2E_TEST_USER_ID** — для E2E-тестов через Telethon. **НЕ** добавлять в `ALLOWED_USER_IDS` — сломает однопользовательский инвариант.

Пример:

```
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
ALLOWED_USER_IDS=123456789
PROJECTS_ROOT_DIR=/home/ivan/projects
```

## Шаг 3: Первый запуск вручную

```bash
source .venv/bin/activate
python -m claude_manager
```

**Ожидаемый результат:** в консоли строки:

```
[INFO] claude_manager.config: Конфигурация загружена: рабочая директория=..., корень проектов=..., пользователей в белом списке=1
[INFO] claude_manager.main: Claude Manager запускается...
```

Бот ждёт сообщений из Telegram. Остановить — `Ctrl+C`.

## Шаг 4: Проверка через Telegram

1. Открыть бота в Telegram (имя задано при создании через @BotFather).
2. Отправить `/new` — бот должен ответить «Создана новая сессия #1».
3. Отправить любое текстовое сообщение — бот передаст в Claude и пришлёт ответ.

Если бот не отвечает — проверить консоль на ошибки авторизации (`Неавторизованный доступ` означает, что ID не в `ALLOWED_USER_IDS`).

## Шаг 5: Автозапуск через systemd

Чтобы бот запускался при входе в систему и перезапускался при падении, используем `systemd --user`.

### Установка unit-файла

```bash
# Создать папку для user-юнитов (если её нет)
mkdir -p ~/.config/systemd/user

# Скопировать unit-файл
cat > ~/.config/systemd/user/claude-manager.service <<'EOF'
[Unit]
Description=Claude Manager Telegram bot
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/ivan/claude-sandbox/claude_manager
Environment=PATH=/home/ivan/.npm-global/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=/home/ivan/claude-sandbox/claude_manager/.venv/bin/claude-manager
Restart=always
RestartSec=10
TimeoutStopSec=30

[Install]
WantedBy=default.target
EOF
```

**Важно:** подправить `WorkingDirectory`, `Environment=PATH=` и `ExecStart` под свои пути. `PATH` должен содержать путь к `node` (узнать через `which node`) — Claude CLI это Node-приложение.

### Перезагрузить systemd и включить автозапуск

```bash
systemctl --user daemon-reload
systemctl --user enable --now claude-manager.service
```

`--now` запускает сервис сразу же, `enable` включает в автозапуск.

### Проверка статуса

```bash
systemctl --user status claude-manager.service
```

Должно быть `active (running)`. Если `failed` — проверить логи:

```bash
journalctl --user -u claude-manager.service -n 50 --no-pager
tail -50 ~/.local/state/claude-manager/claude-manager.log
```

### Команды управления

```bash
systemctl --user start claude-manager.service     # запустить
systemctl --user stop claude-manager.service      # остановить
systemctl --user restart claude-manager.service   # рестарт
systemctl --user disable claude-manager.service   # убрать из автозапуска
systemctl --user status claude-manager.service    # статус
```

Чтобы сервис продолжал работать после выхода пользователя (без активной сессии в TTY) — включить linger:

```bash
sudo loginctl enable-linger $USER
```

## Шаг 6: Запуск с автоперезапуском (для разработки)

Watcher следит за `.py`-файлами в `src/` и шлёт `systemctl --user restart` при изменениях.

```bash
# Установить inotify-tools (один раз)
sudo apt install inotify-tools      # Debian/Ubuntu
sudo dnf install inotify-tools      # Fedora/RHEL

# Сделать скрипт исполняемым (один раз)
chmod +x watch_and_restart.sh

# Запустить
./watch_and_restart.sh
```

Дебаунс 1 секунда защищает от шквала рестартов при сохранении нескольких файлов в IDE. Остановить — `Ctrl+C`.

## Шаг 7: Безопасный рестарт из терминала

`restart-claude-manager.sh` делает preflight (editable install здоров?), `systemctl restart`, post-flight (сервис активен + Python-процесс запущен?). Показывает диагностику при провале.

```bash
chmod +x restart-claude-manager.sh   # один раз
./restart-claude-manager.sh
```

**НЕ запускать из подпроцесса самого бота** (например, через Claude Code Bash tool) — скрипт убьёт собственное дерево процессов, exit 137. Для самоперезапуска — команда `/restart` в Telegram.

## Шаг 8: Настройка E2E-тестов (необязательно)

E2E-тесты через Telethon шлют боту реальные сообщения от тестового аккаунта.

Что нужно:
- Отдельный Telegram-аккаунт (не тот, с которого пишем боту).
- API-ключи Telegram: https://my.telegram.org → API development tools.

```bash
# Установить E2E-зависимости (уже включены в [dev])
pip install -e ".[dev]"
```

Добавить в `.env`:

```
TELETHON_API_ID=ваш_api_id
TELETHON_API_HASH=ваш_api_hash
TELETHON_PHONE=+номер_тестового_аккаунта
TELETHON_BOT_USERNAME=@имя_вашего_бота
E2E_TEST_USER_ID=id_тестового_аккаунта
```

**Важно:** `E2E_TEST_USER_ID` **не** должен совпадать с `ALLOWED_USER_IDS` — это сломает однопользовательский инвариант бота.

Авторизация Telethon (при первом запуске нужен SMS-код):

```bash
python tests/e2e/check_connection.py            # запрос SMS-кода
python tests/e2e/check_connection.py XXXXXX     # ввод кода
python tests/e2e/check_connection.py test       # проверка подключения
```

После успеха session-файл `tests/e2e/telethon_test.session` сохраняется — повторный ввод SMS не нужен.

## Остановка бота

- **Прямой запуск (`python -m claude_manager`):** `Ctrl+C`.
- **Через watcher:** `Ctrl+C` — остановит и watcher, и сервис не тронет (рестарт через systemctl, не через kill).
- **Под systemd:** `systemctl --user stop claude-manager.service`.

## Решение проблем

- **«TELEGRAM_BOT_TOKEN не задан»** — `.env` не найден или поле пустое.
- **«ALLOWED_USER_IDS не задан»** — заполнить поле в `.env`.
- **«PROJECTS_ROOT_DIR не задан»** — заполнить поле, путь к существующей папке.
- **«Бот уже запущен (файл bot.pid заблокирован другим процессом)»** — другая копия активна. Остановить через `systemctl --user stop` или найти процесс через `pgrep -f claude_manager`.
- **«env: node: No such file or directory» в journalctl** — `PATH` в unit-файле не включает путь к `node`. Узнать `which node`, добавить в `Environment=PATH=...`.
- **`claude-manager: command not found` при запуске сервиса** — editable install не сделан. Активировать venv и `pip install -e ".[dev]"`.
- **«Обнаружен конфликт: другой бот уже использует этот токен»** — где-то ещё запущена копия бота с тем же токеном.
```

### Task 14: Точечная правка `dev/docs/brd/brd-user-journeys.md`

**Files:**
- Modify: `dev/docs/brd/brd-user-journeys.md`

- [ ] **Step 1: Заменить упоминания macOS-инфраструктуры**

Найти и заменить (через Edit tool):

В строке ~366:
```markdown
Если бот настроен на автоматический перезапуск через LaunchAgent (системная служба macOS для автозапуска программ) — перезапустится только один экземпляр.
```

На:
```markdown
Если бот настроен на автоматический перезапуск через systemd user service — перезапустится только один экземпляр.
```

В строке ~368:
```markdown
Штатный restart через `restart-claude-manager.sh` считается успешным только после того, как поднялся реальный Python-процесс бота. Живой PID shell-обёртки `start-claude-manager.sh` сам по себе не считается успехом: обёртка может пережидать retry-паузу после startup crash, а пользователь в этот момент всё ещё не получает работающий Telegram-бот.
```

На:
```markdown
Штатный restart через `restart-claude-manager.sh` считается успешным только после того, как поднялся реальный Python-процесс бота. `systemctl --user is-active` сам по себе недостаточен: между «сервис активен» и «Python-бот принимает сообщения» может быть зазор в несколько секунд. Post-flight проверяет оба сигнала: `is-active` и `pgrep -f claude_manager`.
```

В строке ~370 — удалить блок целиком (про stale `last exit code = -15` — это macOS launchd-специфика):
```markdown
Если `launchctl list` показывает старый `last exit code = -15`, но под shell-обёрткой уже есть дочерний Python-процесс `claude_manager`, это не ошибка запуска. `-15` означает, что предыдущий процесс был остановлен сигналом Terminated во время kickstart, а не что новый бот упал.
```

### Task 15: Обновить описания в `dev/docs/docs-index.md`

**Files:**
- Modify: `dev/docs/docs-index.md`

- [ ] **Step 1: Заменить описания двух документов**

Найти строку (39):
```markdown
- [deployment-guide.md](deployment-guide.md) — пошаговая инструкция по развёртыванию бота: виртуальное окружение, .env, LaunchAgent, логи
```

На:
```markdown
- [deployment-guide.md](deployment-guide.md) — пошаговая инструкция по развёртыванию бота под Linux: виртуальное окружение, .env, systemd user service, логи
```

Найти строку (41):
```markdown
- [bot-launch-infrastructure.md](bot-launch-infrastructure.md) — карта всех компонентов запуска бота: что где лежит, цепочка launchd → скрипт → Python, почему venv и скрипт вне Desktop, диагностика TCC/provenance проблем
```

На:
```markdown
- [bot-launch-infrastructure.md](bot-launch-infrastructure.md) — карта всех компонентов запуска бота под Linux: что где лежит, systemd user service, цепочка ExecStart → entry point → Python, /restart через отвязанный subprocess, диагностика через journalctl
```

### Task 16: Закоммитить документацию

> **Дополнительные находки, попавшие в этот же коммит (план не предусмотрел):**
>
> 1. `src/claude_manager/main.py` — 2 комментария с упоминанием LaunchAgent заменены
>    на systemd/journalctl (только комментарии, продакшен-логика не тронута).
> 2. `.env.example` — `PROJECTS_ROOT_DIR` перенесён в раздел «Обязательные настройки»
>    (Phase 1 уже сделала переменную обязательной), убран macOS-default
>    `/Users/ivan/Desktop/claude-sandbox`, примеры путей заменены на `/home/you/...`.
> 3. `tests/integration/test_claude_cli_contract.py` — macOS fallback
>    `/Users/ivan/.npm-global/bin/claude` заменён на универсальный
>    `os.path.expanduser("~/.npm-global/bin/claude")`. Результат: на Linux 2 ранее
>    skip-нутых контрактных теста теперь реально запускаются (1010→1012 passed).
> 4. `dev/docs/brd/brd-user-journeys.md` — 4 дополнительных macOS-упоминания вне плана:
>    «бот работает только на маке» → описание Linux + fcntl + systemd;
>    `~/Library/Logs/claude-manager.error.log` и `~/Library/Logs/claude-manager.log`
>    → XDG-путь `~/.local/state/claude-manager/claude-manager.log`;
>    «Переименование на macOS — мгновенная операция» → «На Linux переименование
>    внутри одной ФС — мгновенная атомарная операция (`rename(2)`)»;
>    `/Users/ivan/claude_manager` в примере алгоритма `_encode_project_path` →
>    `/home/ivan/claude_manager`.
> 5. `CLAUDE.md` — добавлена пропавшая пустая строка между списком и заголовком
>    «## Стандарты разработки скиллов» (поломка markdown форматирования после удаления
>    EDEADLK-буллета).

- [x] **Step 1: Сделать единый коммит по документации**

Run:
```bash
git add CLAUDE.md dev/docs/bot-launch-infrastructure.md dev/docs/deployment-guide.md dev/docs/brd/brd-user-journeys.md dev/docs/docs-index.md
git commit -m "$(cat <<'EOF'
docs: переписать инфраструктурную документацию под Linux

- CLAUDE.md: удалены macOS-only принципы (TCC, ~/.venvs/, retry-обёртка
  launchd, EDEADLK). Обновлены «Принцип verify-before-and-after»,
  «Команды разработки», «Структура проекта», «Принцип обязательного
  editable install».
- bot-launch-infrastructure.md: полностью переписан под systemd user
  service, цепочку ExecStart → entry point, /restart через отвязанный
  subprocess, диагностику через journalctl + XDG-лог.
- deployment-guide.md: полностью переписан под Linux (Ubuntu/Debian/
  Fedora), systemctl --user enable/start, inotify-tools для watcher.
- brd-user-journeys.md: убран launchd-язык в CJM-09 (защита от
  двойного запуска), оставлено бизнес-описание.
- docs-index.md: обновлены описания двух переписанных документов.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Фаза 6. Ручная верификация

### Task 17: Полный pytest-прогон

- [x] **Step 1: Прогнать все тесты ещё раз**

> Финальный прогон после Phase 5 + bonus-правок: **1012 passed, 4 skipped, 1 failed in 102.41s**.
> Единственный fail — унаследованный baseline `test_registry_survives_reload` (Task 0,
> к работе не относится). По сравнению с baseline `1009 passed, 6 skipped, 1 failed`:
> `+3 passed, -2 skipped` — это эффект bonus-правки `test_claude_cli_contract.py`
> (правильный Linux-fallback оживил 2 контрактных теста) и `+1` за тест в Phase 4.

### Task 18: Ручная проверка `restart-claude-manager.sh`

- [ ] **Step 1: Успешный путь**

Run:
```bash
./restart-claude-manager.sh
```

Expected:
- `=== Preflight ===` → `OK: editable install здоров`.
- `=== Restart ===` → `systemctl --user restart отправлен`.
- `=== Post-flight ===` → `OK: claude-manager.service active, MainPID=<число>` за 1-2 попытки.
- `=== Последние строки лога ===` → видны строки запуска.
- `=== Рестарт завершён успешно ===`.

- [ ] **Step 2: Падение в preflight**

Run:
```bash
# Временно скрываем entry point
mv .venv/bin/claude-manager .venv/bin/claude-manager.bak

./restart-claude-manager.sh   # Должен упасть в preflight

# Восстанавливаем
mv .venv/bin/claude-manager.bak .venv/bin/claude-manager
```

Expected: первый запуск — exit 1, сообщение «FAIL: .venv/bin/claude-manager не найден или не исполняем», подсказка «Починка: ... pip install -e».

### Task 19: Ручная проверка `/restart` через Telegram

- [ ] **Step 1: Отправить `/restart` боту в Telegram**

Действия:
1. Открыть Telegram, найти бота.
2. Отправить `/restart`.
3. Получить «Перезапускаюсь через 2 сек...».
4. Подождать 5-10 секунд.
5. Получить «Перезапустился, готов к работе».

Expected: оба сообщения приходят. Если второе не приходит за 30 секунд — проверить `journalctl --user -u claude-manager.service -n 30` и `tail -30 ~/.local/state/claude-manager/claude-manager.log`.

### Task 20: Smoke-тест остальных команд через Telegram

- [ ] **Step 1: Прогнать набор команд**

1. `/new` — создаётся новая сессия, бот отвечает «Создана новая сессия #N».
2. `/sessions` — выводится список сессий.
3. `/projects` — выводится список проектов из `PROJECTS_ROOT_DIR`.
4. `/p1` — переключение в первый проект (если есть).
5. `/agent` — переключение бэкенда (`claude` ↔ `codex`).
6. Текстовое сообщение в активной сессии — бот пересылает в Claude, приходит ответ.
7. `Silence on` / `Silence off` — режим тишины переключается.
8. `/all` — глобальный мониторинг включается. `/p1` — выход обратно в проект.

Expected: все команды работают как раньше. Регрессии — это блокер для финального коммита.

### Task 21: Ручная проверка watch_and_restart.sh

- [ ] **Step 1: Запустить watcher в отдельном терминале**

Run:
```bash
./watch_and_restart.sh
```

Expected: видим «Наблюдатель запущен. Слежу за: .../src» и «Сервис: claude-manager.service».

- [ ] **Step 2: Тронуть .py-файл**

В другом терминале:
```bash
touch src/claude_manager/bot.py
```

Expected: в watcher-терминале появляется строка «Изменения: ...bot.py MODIFY», затем «Перезапускаю claude-manager.service...», затем «Сервис перезапущен».

- [ ] **Step 3: Остановить watcher**

`Ctrl+C` в терминале watcher-а — должен корректно завершиться.

### Task 22: Финальный коммит (если были точечные правки)

- [ ] **Step 1: Проверить статус git**

Run:
```bash
git status
```

- [ ] **Step 2: Если есть несохранённые правки от ручной верификации**

Сделать дополнительный коммит:

```bash
git add <files>
git commit -m "fix: точечные правки после ручной верификации"
```

Если изменений нет — этот шаг пропустить.

---

---

## Диагностика готовности Linux-окружения (28.05 в момент завершения Phase 5)

Проверено непосредственно из работающего бота через `systemctl --user status`:

- **systemd unit** — `~/.config/systemd/user/claude-manager.service` существует, сервис
  `active (running)`. Main PID 210627, запущен 22.05.2026 22:53. Бот работает уже 5 дней.
- **Editable install** — `.venv/bin/claude-manager` существует и исполняемый,
  `python -c "import claude_manager"` проходит без ошибок.
- **XDG-лог** — `~/.local/state/claude-manager/claude-manager.log` уже создан (498 KB),
  последняя запись 28.05.2026 19:35. Phase 2 (XDG-миграция) фактически работает на проде.
- **inotify-tools** — **НЕ установлены** (`which inotifywait` пустой). Перед запуском
  `./watch_and_restart.sh` нужно `sudo apt install inotify-tools` (Debian/Ubuntu)
  или `sudo dnf install inotify-tools` (Fedora). Скрипт сам подсказывает, но лучше
  поставить заранее.
- **Живой бот работает на старом коде** — все 6 коммитов Phase 1-5 (через `fb4846e`,
  `8ebe4cd`, `70067ac` и т.д.) ещё не загружены в память живого процесса (PID 210627
  стартовал 5 дней назад, до коммитов). Новый код начнёт работать только после
  `systemctl --user restart claude-manager.service` или после `/restart` из Telegram.

**Важный нюанс безопасности для самого пользователя.** Я (Claude) сейчас работаю
**внутри** запущенного бота — мой процесс `claude -p ... --effort max` виден в
дереве cgroup `claude-manager.service`. Это значит — я физически не могу вызвать
`./restart-claude-manager.sh` или `systemctl restart` сам: они убьют моё дерево
процессов (`exit 137`, описано в «Принципе запрета самоперезапуска» в CLAUDE.md).
Поэтому Tasks 18, 19, 20, 21 — это ручные действия пользователя, не автоматизация.

---

## Self-review checklist

Перед сдачей плана исполнителю — пройти этот checklist:

**1. Spec coverage.** Каждая секция спеки покрыта задачей?

- Контекст и Цель → задачи 1-22 в сумме реализуют все цели.
- Архитектурное решение для `/restart` → Task 5, Task 6 (тесты + код).
- Изменения в Python (in-place) → Task 1-6.
- Изменения в shell-скриптах (удалить + переписать) → Task 7-10.
- Изменения в CLAUDE.md → Task 11.
- Изменения в документации (bot-launch-infrastructure, deployment-guide, brd, docs-index) → Task 12-16.
- Тестирование (юнит, shell, ручное) → Task 1-3, Task 5-6, Task 9, Task 17-21.
- Контракты с внешними системами → встроены в Task 6, Task 8, Task 10 (systemd, inotify, journalctl).
- Риски (D-Bus, удаление DEFAULT, тесты на удалённые константы) → митигации встроены в Task 3 (ConfigError с понятным сообщением), Task 19 (ручная проверка `/restart`), Task 6 Step 4 (полный pytest для отлова косвенных падений).
- Порядок реализации (15 шагов в спеке) → раскрыт в 22 задачи + baseline.

**2. Placeholder scan.** В плане нет «TBD», «доделать позже», «аналогично Task N». Каждый блок кода полный.

**3. Type consistency.**
- Константа `RESTART_DELAY_BEFORE_SYSTEMCTL_SECONDS` — одно имя везде (Task 6 Step 1 и Step 2 используют его).
- Хелпер `check_editable_install` принимает первый аргумент `venv_bin` — Task 8 определяет, Task 9 тестирует с тем же сигнатурой.
- `service_is_running`, `print_diagnostics_on_failure` — сигнатуры в Task 8 и Task 9 согласованы.
- Имя сервиса `claude-manager.service` — одно и то же в bot.py (Task 6), restart-claude-manager.sh (Task 8), watch_and_restart.sh (Task 10), systemd unit (Task 13).

**4. Размер шагов.** Каждый Step — 2-5 минут. Самые длинные — переписывание документов (Task 12 Step 1, Task 13 Step 1) — это копи-пейст готового текста, поэтому укладывается в 5 минут.

**5. Коммиты.** Логически осмысленные группы: config (Task 3), main (Task 4), bot (Task 6), скрипты (Task 10 Step 4), документация (Task 16), финал (Task 22). Каждый коммит компилируется и проходит тесты на момент создания.

Если в плане найдены проблемы — править на месте, повторного review не нужно.
