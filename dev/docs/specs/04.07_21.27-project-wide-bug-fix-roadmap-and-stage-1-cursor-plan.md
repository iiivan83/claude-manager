# План исправления 89 багов полного ревью: роудмап + детальный этап 1 (кластер курсоров)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Закрыть 89 багов из реестра `dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md`, начиная с двух P1 про молчаливую потерю/лавину сообщений; этот документ — мастер-роудмап всех этапов плюс полностью детализированные TDD-задачи этапа 1 (кластер курсоров: P1-1, P1-2, P2-26).

**Architecture:** Баги чинятся кластерами с общим корнем, а не по одному. Корень этапа 1 — смешение двух видов курсоров: raw-курсор (счёт непустых строк JSONL-файла, 1-based `raw_record_index`) и parsed-курсор (`last_delivered_idx` — индекс в списке распарсенных сообщений, 0-based). Лёгкое cursor-чтение (`read_session_file_cursor`) не парсит историю и помечает это sentinel-значением `CURSOR_ONLY_PARSED_MESSAGE_COUNT = -1`, но sentinel живёт только внутри поллера и теряется при сохранении в `unread_buffer` — дальше `-1` трактуется как parsed-индекс и срезает `messages[0:]` (вся история в чат). Фикс: sentinel становится частью контракта `SessionUnreadState`, все потребители различают cursor-only и parsed состояния; /all перестаёт продвигать raw-курсор мимо придержанного финала; читатели перестают считать недописанную последнюю строку файла.

**Tech Stack:** Python 3.13, pytest (asyncio_mode=auto), dataclasses. Тесты юнит-уровня зеркалят структуру `src/`.

---

## Как работать по этому плану

- **Реестр деталей** — `dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md`: у каждого бага там файлы/строки, сценарий, вердикт верификации. Этот план не дублирует описания — только порядок, кластеры и готовые задачи этапа 1.
- **TDD всегда:** сначала тест, воспроизводящий баг (и падающий), затем минимальный фикс, затем полный прогон.
- **Полный прогон:** `source .venv/bin/activate && python -m pytest tests/ -v` (юнит + интеграционные; e2e не входят в обязательный прогон этапа).
- **Коммиты:** один тематический коммит на задачу, стейджить поимённо (`git add <путь>`), без `git add -A`. Работа прямо в `main` (глобальное правило Work In Main).
- **Отметки прогресса:** после фикса — галочка в чекбоксе роудмапа ниже И пометка «✅ исправлено DD.MM.YYYY» в реестре находок (см. Task 6).
- **Рестарт бота** после слияния фиксов — ТОЛЬКО из терминала `./restart-claude-manager.sh` или командой `/restart` в самом боте. Никогда из Bash-подпроцесса бота (принцип запрета самоперезапуска, инцидент 22-04-2026).
- **Детализация следующих этапов:** перед стартом этапа 2 написать такой же детальный план по актуальному коду (ранние фиксы меняют код, детали заранее устареют). Роудмап ниже фиксирует только состав и порядок.
- **Размер файлов (правило активной проверки):** `all_projects_monitor.py` — 731 строка (уже за stop-порогом 700), `coding_agent_session_file_poller.py` — 692 (у порога, превышение задокументировано в его докстринге). Правки этапа 1 — маленькие точечные фиксы, что допустимо за stop-порогом, но НЕ добавлять в эти файлы новую функциональность; при каждой правке сообщать пользователю текущий размер и рост.

---

## Роудмап: 8 этапов, 89 багов

### Этап 1 — кластер курсоров (детально в этом документе)

Файлы-эпицентры: `coding_agent_backend.py`, `coding_agent_session_file_poller.py`, `unread_buffer.py`, `project_pending_delivery.py`, `all_projects_monitor.py`, `project_manager.py`, `claude_code_session_file_reader.py`, `codex_session_file_reader.py`.

- [x] **P1-1** — курсор-заглушка `-1` попадает в unread_buffer → при входе в проект вываливается вся история сессии — ✅ 04.07.2026
- [x] **P1-2** — /all продвигает raw-курсор мимо придержанного «turn-active» сообщения → финалы теряются навсегда — ✅ 04.07.2026
- [x] **P2-26** — `raw_record_count` считает недописанную последнюю строку → запись, дописанная после baseline, не доставляется никогда — ✅ 04.07.2026

### Этап 2 — честная диагностика ошибок CLI (один новый тип исключения закрывает кластер)

Файлы-эпицентры: `process_lifecycle.py`, `process_backend_send.py`, `process_retry.py`, `process_state.py`, `process_types.py`, `claude_interaction.py`, `reply_route_handler.py`.

- [ ] **P2-15** — сбой ЗАПУСКА CLI сообщается как «агент занят, подождите или /stop»
- [ ] **P2-16** — `_restart_process` вне try → сбой рестарта обрывает весь retry-цикл
- [ ] **P2-14** — alias-резолюция не находит tuple-алиасы CLAUDE-remap'ов (/stop, is_busy, has_process)
- [ ] **P2-17** — busy-гейт под `_busy_lock` использует сырой ключ без alias-резолюции

По касанию (те же файлы): [ ] P3-21 (мёртвый fallback `_check_stop_requested` для CLAUDE), [ ] P3-22 (`session_id_callback` только для первой смены id), [ ] P3-23 (сломанный legacy-путь отправки), [ ] P3-24 (`create_process` под строковыми ключами), [ ] P3-25 (мёртвые записи в `_processes`), [ ] P3-26 (гонка `send_signal` с завершением), [ ] P3-27 (temp-id `_new_*` в дневном реестре), [ ] P3-28 (одноразовый `ClaudeProcess` в legacy-переиспользовании), [ ] P3-29 (строка stdout >16 МБ / обрыв UTF-8), [ ] P3-30 (утечка сабпроцесса при падении первой записи в stdin).

### Этап 3 — жизненный цикл хода (finally проверяет владение)

Файлы-эпицентры: `claude_interaction.py`, `coding_agent_session_file_poller.py`, `process_events.py`.

- [ ] **P2-19** — флаг `handler_owns_final_delivery` протекает после /stop или ошибки
- [ ] **P2-20** — finally первого хода отменяет watchdog и снимает паузу watcher'а второго хода

По касанию: [ ] P3-31 (`_on_session_id_changed` не атомарен — finally резюмит старый id).

### Этап 4 — UnicodeDecodeError во всех читателях JSONL (механический кластер)

Файлы-эпицентры: `claude_code_session_file_reader.py`, `session_reader.py`, `codex_session_index.py`, `codex_session_file_listing.py`, `codex_session_file_reader.py`, `codex_session_metadata.py`, `reply_route_registry.py`.

- [ ] **P2-23** — UnicodeDecodeError от дописываемых файлов роняет /pN (Claude-читатели)
- [ ] **P2-24** — та же дыра в Codex-читателях + `gather` без `return_exceptions`
- [ ] **P3-50** — UnicodeDecodeError при загрузке `reply_routes.json` отключает персистентность маршрутов

### Этап 5a — доставка сообщений в Telegram

Файлы-эпицентры: `telegram_response_delivery.py`, `file_delivery.py`, `message_splitter.py`, `telegram_sender.py`, `file_sender.py`, `telegram_file_downloader.py`.

- [ ] **P2-1** — заголовок сессии добавляется после разбивки на 4096 → сообщение теряется
- [ ] **P2-2** — доставка файлов мимо retry-логики роняет весь финальный ответ
- [ ] **P2-3** — regex курсива `\*(.+?)\*` портит SQL/глобы/арифметику
- [ ] **P2-4** — RetryAfter: одна повторная попытка, молчаливая потеря части сообщения
- [ ] **P2-5** — `[SHOW_FILE]` большого файла замораживает event loop на десятки секунд

По касанию: [ ] P3-10 (разрез на границе HTML-тега), [ ] P3-11 (regex маркеров обрывается на `]`), [ ] P3-12 (`generate_file_name` игнорирует `original_name`), [ ] P3-13 (блокирующий `clean_old_received_files`), [ ] P3-14 (`project_name` без `html.escape`).

### Этап 5b — обработчики Telegram-команд

Файлы-эпицентры: `bot.py`, `telegram_input_handlers.py`, `telegram_session_handlers.py`, `telegram_project_handlers.py`, `main.py`.

- [ ] **P2-6** — редактирование сообщения роняет обработчик (edited_message без гарда)
- [ ] **P2-7** — `/N` внутри /all не гардится → гибридное полусостояние

По касанию: [ ] P3-15 (исключения в /pN и /3s12 молча выбрасывают из /all), [ ] P3-16 (`resume_all` в finally без `has_enabled_chats`), [ ] P3-17 (`unbind_session` без try/except глотает подтверждение), [ ] P3-18 (голый `asyncio.create_task`), [ ] P3-19 (номер сессии в подтверждении `/PsS`), [ ] P3-20 (сообщение об ошибке блокировки называет `bot.pid`).

### Этап 5c — переключение проектов и персистентность

Файлы-эпицентры: `project_manager.py`, `daily_session_registry.py`, `project_pending_delivery.py`, `unread_buffer.py`, `silence_mode_registry.py`.

- [ ] **P2-8** — переключение проекта не вызывает `daily_session_registry.reset_state()` → реестр пишется в файл чужого проекта
- [ ] **P2-9** — `deliver_pending_messages` очищает снапшот без подтверждения доставки
- [ ] **P2-10** — выход из /all в тот же проект → тройная доставка

По касанию: [ ] P3-1 (нечисловой ключ номера — ValueError), [ ] P3-2 (TOCTOU нумерации /pN), [ ] P3-3 (orphan-cleanup без grace-периода), [ ] P3-4 (не-dict JSON в silence mode), [ ] P3-5 (синхронный I/O silence mode), [ ] P3-6 (TTL от `datetime.now()` вместо monotonic), [ ] P3-7 (снапшот до паузы watcher'а).

### Этап 5d — watcher и /all (остальное)

Файлы-эпицентры: `coding_agent_session_file_poller.py`, `all_projects_monitor.py`.

- [ ] **P2-11** — ожившая сессия старше lookback-окна вываливает всю историю broadcast'ом
- [ ] **P2-12** — один поллинг без сессии в листинге удаляет живой курсор
- [ ] **P2-13** — список кандидатов /all заморожен и только сжимается

По касанию: [ ] P3-8 (нет поэлементной изоляции ошибок доставки /all), [ ] P3-9 (ветка «наследуем курсор watcher'а» не отматывает baseline-mtime).

### Этап 5e — инфраструктура процессов (не закрытое этапом 2)

Файлы-эпицентры: `claude_runner.py`.

- [ ] **P2-21** — stderr-пайп не вычитывается во время хода → дедлок «болтливого» CLI

### Этап 5f — бэкенды и читатели файлов сессий

Файлы-эпицентры: `codex_backend.py`, `codex_session_file_listing.py`, `codex_session_index_paths.py`, `claude_code_backend.py`, `codex_session_file_reader.py`, `session_reader.py`, `openai_transcription.py`, `current_backend_registry.py`.

- [ ] **P2-22** — CodexBackend без `classify_permanent_error` → 10 обречённых ретраев
- [ ] **P2-25** — Codex lookback по дате создания, а не активности → возобновлённые сессии невидимы

По касанию: [ ] P3-32 (сырой TimeoutError транскрипции), [ ] P3-33 (не-dict JSON в current-backend), [ ] P3-34 (`is_turn_terminal_session_record` только по result), [ ] P3-35 (Codex cursor-reader сдаётся на неразобранной строке), [ ] P3-36 (UUIDv7 next-second rollover → полный обход дерева), [ ] P3-37 (`datetime.fromtimestamp` без гарда), [ ] P3-38 (незащищённый getmtime-sort), [ ] P3-39 (только первый text-блок), [ ] P3-40 (prefix-fallback без дизамбигуации), [ ] P3-41 (кэш codex-индекса растёт по дню за день), [ ] P3-42 (session_reader — расходящийся дубликат).

### Этап 5g — reply-роутинг и недавние сессии

Файлы-эпицентры: `reply_route_handler.py`, `reply_route_registry.py`, `reply_anchor_registry.py`, `recent_sessions_refresh.py`, `recent_sessions_store.py`, `telegram_session_handlers.py`, `session_request_preview.py`.

- [ ] **P2-18** — роутинговые отправки без `session_id_callback`, rebind до подтверждения приёма
- [ ] **P2-27** — глобальный refresh затирает preview пустыми строками при каждом /all
- [ ] **P2-28** — /stop во время роутинговой отправки даёт ложное «не удалось передать»
- [ ] **P2-29** — refresh не помечает исчезнувшие файлы → «призрачные» строки в /sessions
- [ ] **P2-30** — ответ на кросс-проектный reply теряется: pending-сбор требует существующего снапшота

По касанию: [ ] P3-43 (дрейф спеки reply-routing-v1), [ ] P3-44 (полный refresh при тёплом кэше), [ ] P3-45 (перечитывание preview без сравнения mtime), [ ] P3-46 (store и `_write_lock` на каждый вызов), [ ] P3-47 (busy-гонка двух reply), [ ] P3-48 (устаревший `project_number` в `_full_link`), [ ] P3-49 (дрейф спеки fall-through), [ ] P3-51 (синхронная запись маршрутов на каждый chunk), [ ] P3-52 (реестр reply-якорей растёт неограниченно), [ ] P3-53 (`_hide_subagent_rows` открывает rollout на каждый запрос), [ ] P3-54 (полное чтение файла при preview с `...`), [ ] P3-55 (boilerplate фото-альбомов в preview).

### Этап 6 — документация и латентные хвосты

- [ ] **P3-56** — `media_group_handler` импортирует bot.py в рантайме
- [ ] **P3-57** — дрейф CLAUDE.md: ~23 модуля не описаны, session_watcher описан как реализация
- [ ] Провести ревизию: латентные пункты (P3-23, P3-24, P3-28, P3-34, P3-39, часть P3-22) — починены по касанию на этапах 2/5f или явно зафиксированы как «известный латентный дефект» в реестре

---

# Детальный план этапа 1: кластер курсоров (P1-1, P1-2, P2-26)

## Карта изменений этапа

- Modify: `src/claude_manager/coding_agent_backend.py` — константа `CURSOR_ONLY_PARSED_MESSAGE_COUNT` + поле `parsed_message_count` в `SessionUnreadState`
- Modify: `src/claude_manager/coding_agent_session_file_poller.py` — импорт константы вместо локальной, `get_seen_counts_snapshot` передаёт поле
- Modify: `src/claude_manager/unread_buffer.py` — `save_snapshot` принимает `parsed_message_count`
- Modify: `src/claude_manager/project_manager.py` — `_capture_backend_unread_snapshots` передаёт поле
- Modify: `src/claude_manager/claude_interaction.py` — `_save_last_delivered_message_index_for_later_project_view` передаёт поле
- Modify: `src/claude_manager/project_pending_delivery.py` — cursor-only ветка выбора delta по raw-индексам
- Modify: `src/claude_manager/all_projects_monitor.py` — raw-курсор не съедает придержанный финал; baseline и `_ensure_unread_snapshot` переносят sentinel
- Create: `src/claude_manager/jsonl_incomplete_trailing_line_dropper.py` — отбрасывание недописанной последней строки
- Modify: `src/claude_manager/claude_code_session_file_reader.py`, `src/claude_manager/codex_session_file_reader.py` — применение dropper в snapshot- и cursor-чтениях
- Test: `tests/test_unread_buffer.py`, `tests/test_project_pending_delivery_cursor_semantics.py` (новый), `tests/test_all_projects_monitor.py`, `tests/test_jsonl_incomplete_trailing_line_dropper.py` (новый), `tests/test_claude_code_backend.py`, `tests/test_codex_backend.py`, правки ожиданий в `tests/test_project_manager.py`, `tests/integration/test_project_switching.py`

Имена новых сущностей прошли ревью агента-имён 04.07.2026: `jsonl_incomplete_trailing_line_dropper` (модуль), `drop_incomplete_trailing_jsonl_line`, `_messages_after_raw_cursor`, `_raw_cursor_excluding_held_message`, `test_project_pending_delivery_cursor_semantics.py` — все ПОНЯТНО.

Справка по семантике курсоров (нужна в каждой задаче):

- `raw_record_index` — 1-based номер непустой строки JSONL, присваивается в `parse_jsonl_string_lines` / `_parse_jsonl_string_lines`
- `raw_record_count` — число непустых строк файла; сообщение «новое», если `raw_record_index > <курсор>.raw_record_count`
- `last_delivered_idx` — 0-based индекс в `snapshot.messages` (только распарсенные сообщения)
- `CURSOR_ONLY_PARSED_MESSAGE_COUNT = -1` — метка «parsed-историю не читали, `last_delivered_idx` доверять нельзя»

---

### Task 1: Sentinel `parsed_message_count` становится частью контракта `SessionUnreadState`

**Files:**
- Modify: `src/claude_manager/coding_agent_backend.py:52-58`
- Modify: `src/claude_manager/coding_agent_session_file_poller.py:60, 675-683`
- Modify: `src/claude_manager/unread_buffer.py:43-80`
- Modify: `src/claude_manager/project_manager.py:253-268`
- Modify: `src/claude_manager/claude_interaction.py:190-208`
- Test: `tests/test_unread_buffer.py` (+ правки ожиданий в `tests/test_project_manager.py:383-394`, `tests/integration/test_project_switching.py:253-257`)

- [ ] **Step 1: Написать падающий тест**

В `tests/test_unread_buffer.py` добавить в конец класса с тестами save/restore (стиль файла — методы с `self`), plus импорт `CURSOR_ONLY_PARSED_MESSAGE_COUNT` из `claude_manager.coding_agent_backend` в шапку файла:

```python
    def test_save_snapshot_preserves_cursor_only_parsed_message_count(self) -> None:
        """Cursor-only sentinel должен переживать сохранение в unread_buffer (P1-1)."""
        save_snapshot(
            SESSION_ID,
            BackendName.CLAUDE,
            raw_record_count=7,
            last_delivered_idx=-1,
            parsed_message_count=CURSOR_ONLY_PARSED_MESSAGE_COUNT,
        )

        restored = restore_snapshot(SESSION_ID, BackendName.CLAUDE)

        assert restored is not None
        assert restored.parsed_message_count == CURSOR_ONLY_PARSED_MESSAGE_COUNT

    def test_save_snapshot_preserves_valid_parsed_message_count(self) -> None:
        """Валидное parsed-число сохраняется как есть."""
        save_snapshot(
            SESSION_ID,
            BackendName.CLAUDE,
            raw_record_count=7,
            last_delivered_idx=4,
            parsed_message_count=5,
        )

        restored = restore_snapshot(SESSION_ID, BackendName.CLAUDE)

        assert restored is not None
        assert restored.parsed_message_count == 5
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `python -m pytest tests/test_unread_buffer.py -v -k parsed_message_count`
Expected: FAIL — `ImportError: cannot import name 'CURSOR_ONLY_PARSED_MESSAGE_COUNT'` (до правки константа живёт только в поллере).

- [ ] **Step 3: Реализация контракта**

В `src/claude_manager/coding_agent_backend.py` перед `class SessionUnreadState` добавить константу и поле:

```python
# Sentinel: parsed-история не читалась (лёгкое cursor-чтение файла) —
# last_delivered_idx нельзя трактовать как индекс распарсенного сообщения.
CURSOR_ONLY_PARSED_MESSAGE_COUNT = -1


@dataclass(frozen=True)
class SessionUnreadState:
    """Backend-neutral unread-delivery cursor for one session file."""

    raw_record_count: int
    last_delivered_idx: int
    last_modified_at: float | None = None
    parsed_message_count: int = CURSOR_ONLY_PARSED_MESSAGE_COUNT
```

Default — сентинел сознательно: забытый call-site отдаст «недоверенное» состояние, и pending-путь уйдёт в безопасную raw-ветку вместо среза всей истории.

В `src/claude_manager/coding_agent_session_file_poller.py`:
- удалить строку 60 `CURSOR_ONLY_PARSED_MESSAGE_COUNT = -1`;
- добавить `CURSOR_ONLY_PARSED_MESSAGE_COUNT` в существующий импорт из `claude_manager.coding_agent_backend` (блок строк 33-40) — имя останется доступным внутри модуля во всех текущих точках использования (строки 317, 466, 570);
- в `get_seen_counts_snapshot` (строки 675-683) передать поле:

```python
    def get_seen_counts_snapshot(self) -> dict[str, SessionUnreadState]:
        return {
            session_id: SessionUnreadState(
                raw_record_count=state.raw_count,
                last_delivered_idx=state.last_delivered_idx,
                last_modified_at=state.last_modified_at,
                parsed_message_count=state.parsed_message_count,
            )
            for session_id, state in self._states.items()
        }
```

В `src/claude_manager/unread_buffer.py`: импортировать константу и добавить параметр в `save_snapshot`:

```python
from claude_manager.coding_agent_backend import (
    BackendName,
    CURSOR_ONLY_PARSED_MESSAGE_COUNT,
    SessionUnreadState,
)
```

```python
def save_snapshot(
    session_id: str,
    backend: BackendName | dict[str, int],
    raw_record_count: int | None = None,
    last_delivered_idx: int | None = None,
    last_modified_at: float | None = None,
    parsed_message_count: int = CURSOR_ONLY_PARSED_MESSAGE_COUNT,
) -> None:
    """Сохраняет cursor-состояние для пары session_id/backend."""
```

и в конструировании state:

```python
    state = SessionUnreadState(
        raw_record_count=raw_record_count,
        last_delivered_idx=last_delivered_idx,
        last_modified_at=last_modified_at,
        parsed_message_count=parsed_message_count,
    )
```

В `src/claude_manager/project_manager.py:258-261` добавить поле в kwargs:

```python
            snapshot_kwargs = {
                "raw_record_count": unread_state.raw_record_count,
                "last_delivered_idx": unread_state.last_delivered_idx,
                "parsed_message_count": unread_state.parsed_message_count,
            }
```

В `src/claude_manager/claude_interaction.py:203-208` добавить поле в вызов:

```python
    unread_buffer.save_snapshot(
        session_id,
        backend,
        raw_record_count=last_seen_position.raw_record_count,
        last_delivered_idx=last_seen_position.last_delivered_idx,
        parsed_message_count=last_seen_position.parsed_message_count,
    )
```

- [ ] **Step 4: Убедиться, что новые тесты проходят**

Run: `python -m pytest tests/test_unread_buffer.py -v`
Expected: PASS все.

- [ ] **Step 5: Полный прогон и правка сломанных equality-ожиданий**

Run: `python -m pytest tests/ -v`

Известные падения (equality-сравнения и assert_any_call не знают про новое поле) — чинить по правилу «добавить `parsed_message_count` с фактическим значением production-пути»:

- `tests/test_project_manager.py:383-394` — `save_mock.assert_any_call(...)`: добавить в оба вызова `parsed_message_count=CURSOR_ONLY_PARSED_MESSAGE_COUNT` (фикстурные `SessionUnreadState` построены без поля → сентинел) и импортировать константу;
- `tests/integration/test_project_switching.py:253, 257` — ожидания `SessionUnreadState(raw_record_count=5, last_delivered_idx=4)` и `(7, 6)`: добавить `parsed_message_count=0` (watcher-состояния в тесте построены как `SessionWatcherState(raw_count=..., last_delivered_idx=...)`, а у `SessionWatcherState.parsed_message_count` default `0`);
- прочие падения этого класса (кандидаты: `tests/test_claude_interaction.py`, `tests/test_all_projects_monitor.py:578`, `tests/test_coding_agent_backend.py`) — та же схема: смотреть, какое значение реально несёт production-путь, и дописывать его в ожидание. Семантику ассертов не ослаблять.

Expected: после правок — PASS все.

- [ ] **Step 6: Commit**

```bash
git add src/claude_manager/coding_agent_backend.py \
        src/claude_manager/coding_agent_session_file_poller.py \
        src/claude_manager/unread_buffer.py \
        src/claude_manager/project_manager.py \
        src/claude_manager/claude_interaction.py \
        tests/test_unread_buffer.py \
        tests/test_project_manager.py \
        tests/integration/test_project_switching.py
git commit -m "fix: sentinel parsed_message_count в контракте SessionUnreadState (этап 1, P1-1 шаг 1)"
```

(Плюс любые другие тестовые файлы, правленные на Step 5, — добавить поимённо.)

---

### Task 2: P1-1 — pending-сбор различает cursor-only и parsed состояния

**Files:**
- Modify: `src/claude_manager/project_pending_delivery.py:191-225`
- Test: Create `tests/test_project_pending_delivery_cursor_semantics.py`

- [ ] **Step 1: Написать падающие тесты**

Создать `tests/test_project_pending_delivery_cursor_semantics.py` целиком:

```python
"""Cursor-semantics tests for project-switch pending collection (P1-1)."""

import pytest

from claude_manager import unread_buffer
from claude_manager.coding_agent_backend import (
    CURSOR_ONLY_PARSED_MESSAGE_COUNT,
    BackendName,
    SessionFileInfo,
    SessionFileSnapshot,
    SessionMessage,
)
from claude_manager.project_pending_delivery import (
    _collect_pending_for_session_file,
)


class CursorAwareFakeBackend:
    """Фейковый backend-адаптер с раздельными cursor- и snapshot-чтениями."""

    name = BackendName.CLAUDE

    def __init__(
        self,
        cursor: SessionFileSnapshot,
        snapshot: SessionFileSnapshot,
    ) -> None:
        self._cursor = cursor
        self._snapshot = snapshot

    async def read_session_file_cursor(self, file_path: str) -> SessionFileSnapshot:
        del file_path
        return self._cursor

    async def read_session_file_snapshot(self, file_path: str) -> SessionFileSnapshot:
        del file_path
        return self._snapshot


@pytest.fixture(autouse=True)
def _clean_unread_buffer():
    unread_buffer._snapshots.clear()
    yield
    unread_buffer._snapshots.clear()


def _assistant_message(text: str, raw_record_index: int | None) -> SessionMessage:
    return SessionMessage(
        role="assistant",
        text=text,
        timestamp=None,
        is_empty_response=False,
        raw_record_index=raw_record_index,
    )


def _file_info(session_id: str) -> SessionFileInfo:
    return SessionFileInfo(
        session_id=session_id,
        file_path=f"/fake/{session_id}.jsonl",
        last_modified_at=100.0,
        preview="preview",
    )


def _snapshot(
    messages: list[SessionMessage],
    raw_record_count: int,
) -> SessionFileSnapshot:
    return SessionFileSnapshot(
        messages=messages,
        raw_record_count=raw_record_count,
        last_record={},
        is_turn_active=False,
    )


async def test_cursor_only_state_delivers_only_messages_after_raw_cursor() -> None:
    """Cursor-only baseline (-1) не вываливает всю историю сессии (P1-1)."""
    unread_buffer.save_snapshot(
        "sess-1",
        BackendName.CLAUDE,
        raw_record_count=3,
        last_delivered_idx=-1,
        parsed_message_count=CURSOR_ONLY_PARSED_MESSAGE_COUNT,
    )
    messages = [
        _assistant_message("старый ответ 1", raw_record_index=1),
        _assistant_message("старый ответ 2", raw_record_index=2),
        _assistant_message("старый ответ 3", raw_record_index=3),
        _assistant_message("новый ответ", raw_record_index=4),
    ]
    backend_adapter = CursorAwareFakeBackend(
        cursor=_snapshot([], raw_record_count=4),
        snapshot=_snapshot(messages, raw_record_count=4),
    )

    pending_items = await _collect_pending_for_session_file(
        backend_adapter,
        _file_info("sess-1"),
    )

    assert [item.text for item in pending_items] == ["новый ответ"]


async def test_cursor_only_state_without_raw_indices_delivers_nothing() -> None:
    """Без raw-индексов отличить новое от старого нельзя — лучше тишина, чем лавина."""
    unread_buffer.save_snapshot(
        "sess-2",
        BackendName.CLAUDE,
        raw_record_count=3,
        last_delivered_idx=-1,
        parsed_message_count=CURSOR_ONLY_PARSED_MESSAGE_COUNT,
    )
    messages = [
        _assistant_message("старый ответ", raw_record_index=None),
        _assistant_message("новый ответ", raw_record_index=None),
    ]
    backend_adapter = CursorAwareFakeBackend(
        cursor=_snapshot([], raw_record_count=4),
        snapshot=_snapshot(messages, raw_record_count=4),
    )

    pending_items = await _collect_pending_for_session_file(
        backend_adapter,
        _file_info("sess-2"),
    )

    assert pending_items == []


async def test_valid_parsed_state_keeps_slice_semantics() -> None:
    """Обычное parsed-состояние доставляет по last_delivered_idx, как раньше."""
    unread_buffer.save_snapshot(
        "sess-3",
        BackendName.CLAUDE,
        raw_record_count=3,
        last_delivered_idx=1,
        parsed_message_count=2,
    )
    messages = [
        _assistant_message("доставлен раньше 1", raw_record_index=1),
        _assistant_message("доставлен раньше 2", raw_record_index=3),
        _assistant_message("новый ответ", raw_record_index=4),
    ]
    backend_adapter = CursorAwareFakeBackend(
        cursor=_snapshot([], raw_record_count=4),
        snapshot=_snapshot(messages, raw_record_count=4),
    )

    pending_items = await _collect_pending_for_session_file(
        backend_adapter,
        _file_info("sess-3"),
    )

    assert [item.text for item in pending_items] == ["новый ответ"]
```

- [ ] **Step 2: Убедиться, что первый тест падает**

Run: `python -m pytest tests/test_project_pending_delivery_cursor_semantics.py -v`
Expected: первый тест FAIL — доставлены все 4 текста (срез `messages[-1 + 1:]`); второй тест FAIL — доставлены оба текста; третий PASS.

- [ ] **Step 3: Реализация**

В `src/claude_manager/project_pending_delivery.py`:

добавить `CURSOR_ONLY_PARSED_MESSAGE_COUNT` в импорт из `claude_manager.coding_agent_backend` (блок строк 18-25);

добавить хелпер перед `_collect_pending_for_session_file`:

```python
def _messages_after_raw_cursor(
    messages: list[SessionMessage],
    raw_record_count: int,
    session_id: str,
) -> list[SessionMessage]:
    """Выбирает сообщения после raw-курсора, когда parsed-индексу доверять нельзя."""
    if not any(message.raw_record_index is not None for message in messages):
        # Без raw-индексов отличить новое от старого нельзя: лучше ничего не
        # доставить, чем вывалить всю историю сессии в чат (P1-1).
        logger.warning(
            "Cursor-only снапшот без raw-индексов: pending-доставка пропущена (%s)",
            session_id,
        )
        return []
    return [
        message
        for message in messages
        if message.raw_record_index is not None
        and message.raw_record_index > raw_record_count
    ]
```

и заменить строку 219 (`delta = snapshot.messages[old_state.last_delivered_idx + 1:]`) на ветвление:

```python
    if old_state.parsed_message_count == CURSOR_ONLY_PARSED_MESSAGE_COUNT:
        delta = _messages_after_raw_cursor(
            snapshot.messages,
            old_state.raw_record_count,
            file_info.session_id,
        )
    else:
        delta = snapshot.messages[old_state.last_delivered_idx + 1:]
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `python -m pytest tests/test_project_pending_delivery_cursor_semantics.py -v`
Expected: PASS все 3.

- [ ] **Step 5: Полный прогон**

Run: `python -m pytest tests/ -v`
Expected: PASS. Если падают тесты pending-доставки, опиравшиеся на срез при сентинеле, — это и есть починенный баг: обновить их ожидания на raw-семантику.

- [ ] **Step 6: Commit**

```bash
git add src/claude_manager/project_pending_delivery.py \
        tests/test_project_pending_delivery_cursor_semantics.py
git commit -m "fix: pending-сбор не вываливает историю при cursor-only снапшоте (P1-1)"
```

---

### Task 3: P1-2 — /all не продвигает raw-курсор мимо придержанного финала

**Files:**
- Modify: `src/claude_manager/all_projects_monitor.py:547-603`
- Test: `tests/test_all_projects_monitor.py`

- [ ] **Step 1: Написать падающие тесты**

Добавить в `tests/test_all_projects_monitor.py` (рядом с существующими тестами `_candidate_indices`, стиль хелперов `_snapshot`/`_raw_message`/`_file` уже в файле; для async-теста нужен импорт `unread_buffer` — он уже в шапке):

```python
def test_next_state_keeps_raw_cursor_before_held_final_message() -> None:
    """Активный ход: raw-курсор не съедает придержанный финал (P1-2)."""
    previous = all_projects_monitor._AllMonitorState(
        raw_record_count=3,
        last_delivered_idx=0,
    )
    snapshot = _snapshot(
        [
            _raw_message("assistant", "старый ответ", raw_record_index=2),
            _raw_message("assistant", "придержанный финал", raw_record_index=5),
        ],
        raw_count=6,
        is_turn_active=True,
    )

    next_state = all_projects_monitor._next_state_from_snapshot(
        previous, snapshot, 50.0,
    )

    # Курсор останавливается ПЕРЕД придержанным сообщением (raw_record_index=5),
    # чтобы на следующем поллинге оно снова стало кандидатом: 5 > 4.
    assert next_state.raw_record_count == 4


def test_held_final_message_is_candidate_again_after_turn_completes() -> None:
    """Финал, придержанный при активном ходе, доставляется после завершения хода."""
    previous = all_projects_monitor._AllMonitorState(
        raw_record_count=3,
        last_delivered_idx=0,
    )
    active_snapshot = _snapshot(
        [
            _raw_message("assistant", "старый ответ", raw_record_index=2),
            _raw_message("assistant", "придержанный финал", raw_record_index=5),
        ],
        raw_count=6,
        is_turn_active=True,
    )
    after_active_turn = all_projects_monitor._next_state_from_snapshot(
        previous, active_snapshot, 50.0,
    )
    completed_snapshot = _snapshot(
        [
            _raw_message("assistant", "старый ответ", raw_record_index=2),
            _raw_message("assistant", "придержанный финал", raw_record_index=5),
        ],
        raw_count=7,
        is_turn_active=False,
    )

    candidate_indices = all_projects_monitor._candidate_indices(
        after_active_turn, completed_snapshot,
    )

    assert candidate_indices == [1]


def test_next_state_advances_raw_cursor_fully_when_turn_completed() -> None:
    """Завершённый ход продвигает raw-курсор до полного счётчика файла."""
    previous = all_projects_monitor._AllMonitorState(
        raw_record_count=3,
        last_delivered_idx=0,
    )
    snapshot = _snapshot(
        [_raw_message("assistant", "финал", raw_record_index=5)],
        raw_count=6,
        is_turn_active=False,
    )

    next_state = all_projects_monitor._next_state_from_snapshot(
        previous, snapshot, 50.0,
    )

    assert next_state.raw_record_count == 6


@pytest.mark.asyncio()
async def test_unread_snapshot_saved_even_when_held_final_blocks_delivery() -> None:
    """Если все кандидаты придержаны, pending-снапшот всё равно фиксируется (P1-2)."""
    unread_buffer._snapshots.clear()
    backend = FakeBackend(BackendName.CLAUDE, {})
    project_session = all_projects_monitor._ProjectSession(
        project_number=1,
        project_name="alpha",
        project_path="/fake/alpha",
        session_number=1,
        file_info=_file("sess-held", "/sessions/held.jsonl", mtime=30.0),
        backend=backend,
    )
    previous = all_projects_monitor._AllMonitorState(
        raw_record_count=3,
        last_delivered_idx=0,
    )
    snapshot = _snapshot(
        [_raw_message("assistant", "придержанный финал", raw_record_index=5)],
        raw_count=6,
        is_turn_active=True,
    )
    callback = AsyncMock()

    await all_projects_monitor._deliver_project_session_delta(
        [CHAT_ID], project_session, snapshot, previous, callback,
    )

    callback.assert_not_called()
    stored = unread_buffer.restore_snapshot("sess-held", BackendName.CLAUDE)
    assert stored is not None
    assert stored.raw_record_count == 3
    assert stored.last_delivered_idx == 0
    unread_buffer._snapshots.clear()
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `python -m pytest tests/test_all_projects_monitor.py -v -k "held or next_state"`
Expected: FAIL `test_next_state_keeps_raw_cursor_before_held_final_message` (курсор 6 вместо 4), FAIL `test_held_final_message_is_candidate_again_after_turn_completes` (кандидатов нет), FAIL `test_unread_snapshot_saved_even_when_held_final_blocks_delivery` (снапшот None); PASS `test_next_state_advances_raw_cursor_fully_when_turn_completed`.

- [ ] **Step 3: Реализация**

В `src/claude_manager/all_projects_monitor.py` добавить хелпер перед `_next_state_from_snapshot` и переписать обе функции:

```python
def _raw_cursor_excluding_held_message(
    previous: _AllMonitorState,
    snapshot: SessionFileSnapshot,
) -> int:
    """Возвращает raw-курсор, не съедающий придержанный финал активного хода."""
    held_message = snapshot.messages[-1]
    if held_message.raw_record_index is None:
        return snapshot.raw_record_count
    if held_message.raw_record_index <= previous.raw_record_count:
        # Последнее сообщение уже было за курсором раньше — придерживать нечего.
        return snapshot.raw_record_count
    return min(snapshot.raw_record_count, held_message.raw_record_index - 1)


def _next_state_from_snapshot(
    previous: _AllMonitorState,
    snapshot: SessionFileSnapshot,
    last_modified_at: float,
) -> _AllMonitorState:
    """Build the next all-mode cursor after a snapshot was processed."""
    if snapshot.is_turn_active and snapshot.messages:
        last_delivered_idx = max(
            previous.last_delivered_idx,
            len(snapshot.messages) - 2,
        )
        # Финал придержан _candidate_indices — raw-курсор обязан остановиться
        # перед ним, иначе на следующем поллинге финал уже не кандидат (P1-2).
        raw_record_count = _raw_cursor_excluding_held_message(previous, snapshot)
    else:
        last_delivered_idx = len(snapshot.messages) - 1
        raw_record_count = snapshot.raw_record_count

    return _AllMonitorState(
        raw_record_count=raw_record_count,
        parsed_message_count=len(snapshot.messages),
        last_delivered_idx=last_delivered_idx,
        is_turn_active=snapshot.is_turn_active,
        last_modified_at=last_modified_at,
    )
```

В `_deliver_project_session_delta` перенести сохранение unread-снапшота ДО фильтра deliverable (замена строк 570-603 до цикла доставки):

```python
async def _deliver_project_session_delta(
    chat_ids: list[int],
    project_session: _ProjectSession,
    snapshot: SessionFileSnapshot,
    previous: _AllMonitorState,
    callback: AllProjectsMessageCallback,
) -> None:
    """Deliver new assistant messages from one project session."""
    candidate_indices = _candidate_indices(previous, snapshot)
    if not candidate_indices:
        return

    file_info = project_session.file_info
    backend_name = project_session.backend.name
    # Снапшот фиксируется до deliverable-фильтра: даже если все кандидаты
    # придержаны активным ходом, pending-путь проекта должен получить
    # до-/all курсор — иначе придержанный финал потеряется и там (P1-2).
    _ensure_unread_snapshot(file_info.session_id, backend_name, previous)

    deliverable = [
        (index, snapshot.messages[index])
        for index in candidate_indices
        if _message_should_be_delivered(snapshot.messages[index])
    ]
    if not deliverable:
        return
```

(цикл `for position, (_index, message) in enumerate(deliverable):` ниже — без изменений).

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `python -m pytest tests/test_all_projects_monitor.py tests/test_all_projects_monitor_missing_reappear.py -v`
Expected: PASS все.

- [ ] **Step 5: Полный прогон**

Run: `python -m pytest tests/ -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/claude_manager/all_projects_monitor.py tests/test_all_projects_monitor.py
git commit -m "fix: /all не продвигает raw-курсор мимо придержанного финала (P1-2)"
```

---

### Task 4: /all baseline и `_ensure_unread_snapshot` переносят sentinel (закрывает /all-путь P1-1)

**Files:**
- Modify: `src/claude_manager/all_projects_monitor.py:311-352, 530-544`
- Test: `tests/test_all_projects_monitor.py`

- [ ] **Step 1: Написать падающие тесты**

Добавить в `tests/test_all_projects_monitor.py` (импортировать `CURSOR_ONLY_PARSED_MESSAGE_COUNT` из `claude_manager.coding_agent_backend` в шапку):

```python
def test_baseline_from_cursor_only_read_uses_parsed_count_sentinel() -> None:
    """Baseline из cursor-чтения не выдаёт пустой messages за parsed-историю (P1-1)."""
    backend = FakeBackend(BackendName.CLAUDE, {})
    project_session = all_projects_monitor._ProjectSession(
        project_number=1,
        project_name="alpha",
        project_path="/fake/alpha",
        session_number=1,
        file_info=_file("sess-baseline", "/sessions/baseline.jsonl", mtime=10.0),
        backend=backend,
    )
    cursor_snapshot = _snapshot([], raw_count=42)

    state = all_projects_monitor._baseline_state_from_snapshot(
        project_session, cursor_snapshot, None, None,
    )

    assert state.parsed_message_count == CURSOR_ONLY_PARSED_MESSAGE_COUNT
    assert state.raw_record_count == 42
    assert state.last_delivered_idx == -1


def test_baseline_inherits_parsed_count_from_watcher_state() -> None:
    """Baseline, унаследованный от watcher'а, сохраняет его parsed-число."""
    backend = FakeBackend(BackendName.CLAUDE, {})
    project_session = all_projects_monitor._ProjectSession(
        project_number=1,
        project_name="alpha",
        project_path="/fake/alpha",
        session_number=1,
        file_info=_file("sess-watcher", "/sessions/watcher.jsonl", mtime=10.0),
        backend=backend,
    )
    watcher_state = SessionUnreadState(
        raw_record_count=5,
        last_delivered_idx=4,
        parsed_message_count=5,
    )
    cursor_snapshot = _snapshot([], raw_count=7)

    state = all_projects_monitor._baseline_state_from_snapshot(
        project_session, cursor_snapshot, watcher_state, None,
    )

    assert state.parsed_message_count == 5
    assert state.last_delivered_idx == 4


@pytest.mark.asyncio()
async def test_ensure_unread_snapshot_passes_parsed_message_count() -> None:
    """Unread-снапшот из /all несёт parsed-число состояния, а не default (P1-1)."""
    unread_buffer._snapshots.clear()
    previous = all_projects_monitor._AllMonitorState(
        raw_record_count=6,
        parsed_message_count=3,
        last_delivered_idx=2,
    )

    all_projects_monitor._ensure_unread_snapshot(
        "sess-parsed", BackendName.CLAUDE, previous,
    )

    stored = unread_buffer.restore_snapshot("sess-parsed", BackendName.CLAUDE)
    assert stored is not None
    assert stored.parsed_message_count == 3
    unread_buffer._snapshots.clear()
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `python -m pytest tests/test_all_projects_monitor.py -v -k "baseline_from_cursor or inherits_parsed or passes_parsed"`
Expected: FAIL `test_baseline_from_cursor_only_read_uses_parsed_count_sentinel` (parsed 0 вместо -1), FAIL `test_baseline_inherits_parsed_count_from_watcher_state` (parsed 0 вместо 5), FAIL `test_ensure_unread_snapshot_passes_parsed_message_count` (сентинел вместо 3).

- [ ] **Step 3: Реализация**

В `src/claude_manager/all_projects_monitor.py` добавить `CURSOR_ONLY_PARSED_MESSAGE_COUNT` в импорт из `claude_manager.coding_agent_backend` (блок строк 28-34) и переписать `_baseline_state_from_snapshot`:

```python
def _baseline_state_from_snapshot(
    project_session: _ProjectSession,
    snapshot: SessionFileSnapshot,
    watcher_state: object | None,
    previous_all_mode_state: _AllMonitorState | None,
) -> _AllMonitorState:
    """Build an all-mode baseline cursor from one session-file snapshot."""
    last_delivered_idx = len(snapshot.messages) - 1
    raw_record_count = snapshot.raw_record_count
    # Baseline читается cursor-чтением без parsed-истории: пустой messages —
    # это «не читали», а не «0 сообщений» (P1-1).
    parsed_message_count = (
        len(snapshot.messages)
        if snapshot.messages
        else CURSOR_ONLY_PARSED_MESSAGE_COUNT
    )
    baseline_modified_at = project_session.file_info.last_modified_at

    if watcher_state is not None:
        last_delivered_idx = getattr(watcher_state, "last_delivered_idx")
        raw_record_count = getattr(watcher_state, "raw_record_count")
        parsed_message_count = getattr(
            watcher_state,
            "parsed_message_count",
            CURSOR_ONLY_PARSED_MESSAGE_COUNT,
        )
    elif previous_all_mode_state is not None:
        # All mode already delivered up to this cursor during an earlier session.
        # Resume from its own progress so re-entering /all does not re-show those
        # messages, and leave the unread snapshot untouched for the real-project
        # pending path that consumes it on return.
        last_delivered_idx = previous_all_mode_state.last_delivered_idx
        raw_record_count = previous_all_mode_state.raw_record_count
        parsed_message_count = previous_all_mode_state.parsed_message_count
        baseline_modified_at = previous_all_mode_state.last_modified_at
    else:
        unread_state = unread_buffer.restore_snapshot(
            project_session.file_info.session_id,
            project_session.backend.name,
        )
        if unread_state is not None:
            # Resume from the cursor a project switch saved earlier and rewind
            # the baseline mtime, so the next poll re-reads the file and delivers
            # the backlog the user had not yet seen in that other project.
            last_delivered_idx = unread_state.last_delivered_idx
            raw_record_count = unread_state.raw_record_count
            parsed_message_count = unread_state.parsed_message_count
            baseline_modified_at = unread_state.last_modified_at or 0.0

    return _AllMonitorState(
        raw_record_count=raw_record_count,
        parsed_message_count=parsed_message_count,
        last_delivered_idx=last_delivered_idx,
        is_turn_active=snapshot.is_turn_active,
        last_modified_at=baseline_modified_at,
    )
```

В `_ensure_unread_snapshot` (строки 530-544) добавить поле:

```python
    unread_buffer.save_snapshot(
        session_id,
        backend,
        raw_record_count=previous.raw_record_count,
        last_delivered_idx=previous.last_delivered_idx,
        parsed_message_count=previous.parsed_message_count,
    )
```

Примечание: у `_AllMonitorState.parsed_message_count` default остаётся `0` — это внутренний тип /all, его состояния всегда строятся из snapshot-чтений; sentinel в него попадает только из baseline-веток выше.

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `python -m pytest tests/test_all_projects_monitor.py -v`
Expected: PASS все. Если `test_poll_delivers_all_project_message_and_keeps_unread_snapshot` (строка ~526, ассерт на строке ~578 сравнивает с `SessionUnreadState(...)`) упал — добавить в ожидание `parsed_message_count` с фактическим значением состояния из этого теста.

- [ ] **Step 5: Полный прогон**

Run: `python -m pytest tests/ -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/claude_manager/all_projects_monitor.py tests/test_all_projects_monitor.py
git commit -m "fix: /all baseline и unread-снапшот переносят cursor-only sentinel (P1-1, путь /all)"
```

---

### Task 5: P2-26 — читатели не считают недописанную последнюю строку

**Files:**
- Create: `src/claude_manager/jsonl_incomplete_trailing_line_dropper.py`
- Modify: `src/claude_manager/claude_code_session_file_reader.py:430-535`
- Modify: `src/claude_manager/codex_session_file_reader.py:240-316`
- Test: Create `tests/test_jsonl_incomplete_trailing_line_dropper.py`; add to `tests/test_claude_code_backend.py`, `tests/test_codex_backend.py`

- [ ] **Step 1: Написать падающие юнит-тесты хелпера**

Создать `tests/test_jsonl_incomplete_trailing_line_dropper.py` целиком:

```python
"""Tests for dropping an incomplete trailing JSONL line (P2-26)."""

from claude_manager.jsonl_incomplete_trailing_line_dropper import (
    drop_incomplete_trailing_jsonl_line,
)


def test_torn_last_line_without_newline_is_dropped() -> None:
    raw_lines = ['{"type": "assistant"}\n', '{"type": "assi']

    assert drop_incomplete_trailing_jsonl_line(raw_lines) == [
        '{"type": "assistant"}\n',
    ]


def test_complete_json_last_line_without_newline_is_kept() -> None:
    """Валидный JSON без завершающего перевода строки — дописанная запись."""
    raw_lines = ['{"type": "assistant"}\n', '{"type": "result"}']

    assert drop_incomplete_trailing_jsonl_line(raw_lines) == raw_lines


def test_last_line_with_newline_is_kept_even_if_invalid_json() -> None:
    # Строка с '\n' дописана полностью: если это не JSON — это мусор, который
    # парсер пропустит штатно, но из raw-счётчика её убирать нельзя.
    raw_lines = ['{"type": "assistant"}\n', 'not json at all\n']

    assert drop_incomplete_trailing_jsonl_line(raw_lines) == raw_lines


def test_empty_lines_list_is_returned_as_is() -> None:
    assert drop_incomplete_trailing_jsonl_line([]) == []
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `python -m pytest tests/test_jsonl_incomplete_trailing_line_dropper.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'claude_manager.jsonl_incomplete_trailing_line_dropper'`.

- [ ] **Step 3: Создать модуль**

Создать `src/claude_manager/jsonl_incomplete_trailing_line_dropper.py` целиком:

```python
"""Отбрасывание недописанной последней строки JSONL-файла сессии.

CLI дописывает файл сессии на лету: последняя строка может быть оборвана
посреди JSON-записи (без завершающего перевода строки). Если посчитать её в
raw_record_count, дописанная позже запись получит уже «прочитанный» raw-индекс
и не будет доставлена никогда (P2-26 полного ревью 04.07.2026).
"""

import json


def drop_incomplete_trailing_jsonl_line(raw_lines: list[str]) -> list[str]:
    """Убирает последнюю строку без перевода строки, если она не парсится как JSON."""
    if not raw_lines:
        return raw_lines
    last_line = raw_lines[-1]
    if last_line.endswith("\n"):
        return raw_lines
    stripped_last_line = last_line.strip()
    if not stripped_last_line:
        return raw_lines
    try:
        json.loads(stripped_last_line)
    except json.JSONDecodeError:
        return raw_lines[:-1]
    # Без '\n', но валидный JSON: запись завершена, просто writer не добавил
    # перевод строки после финальной записи файла.
    return raw_lines
```

Run: `python -m pytest tests/test_jsonl_incomplete_trailing_line_dropper.py -v`
Expected: PASS все 4.

- [ ] **Step 4: Написать падающие reproduce-тесты обоих бэкендов**

В `tests/test_claude_code_backend.py` добавить (хелпер `write_jsonl_file` и фикстура `backend` уже в файле):

```python
async def test_torn_trailing_line_is_not_counted_in_raw_record_count(
    backend: ClaudeCodeBackend,
    tmp_path: Path,
) -> None:
    """Недописанная последняя строка не входит в raw_record_count (P2-26)."""
    session_file = tmp_path / "torn_tail.jsonl"
    write_jsonl_file(
        session_file,
        [{"type": "user", "message": {"content": "вопрос"}}],
    )
    with session_file.open("a", encoding="utf-8") as file_handle:
        file_handle.write('{"type": "assistant", "message": {"content": [{"ty')

    cursor = await backend.read_session_file_cursor(str(session_file))
    snapshot = await backend.read_session_file_snapshot(str(session_file))

    assert cursor.raw_record_count == 1
    assert snapshot.raw_record_count == 1


async def test_record_completed_after_torn_baseline_is_delivered_by_raw_index(
    backend: ClaudeCodeBackend,
    tmp_path: Path,
) -> None:
    """Запись, дописанная после baseline, получает raw-индекс ЗА курсором (P2-26)."""
    session_file = tmp_path / "torn_then_completed.jsonl"
    write_jsonl_file(
        session_file,
        [{"type": "user", "message": {"content": "вопрос"}}],
    )
    with session_file.open("a", encoding="utf-8") as file_handle:
        file_handle.write('{"type": "assistant", "message": {"content": [{"ty')

    baseline_cursor = await backend.read_session_file_cursor(str(session_file))

    with session_file.open("a", encoding="utf-8") as file_handle:
        file_handle.write('pe": "text", "text": "ответ"}]}}\n')

    snapshot = await backend.read_session_file_snapshot(str(session_file))
    newly_visible = [
        message
        for message in snapshot.messages
        if message.raw_record_index is not None
        and message.raw_record_index > baseline_cursor.raw_record_count
    ]

    assert [message.text for message in newly_visible] == ["ответ"]
```

В `tests/test_codex_backend.py` добавить (свой `write_jsonl_file` и фикстура `backend: CodexBackend` уже в файле):

```python
async def test_torn_trailing_line_is_not_counted_in_codex_raw_record_count(
    backend: CodexBackend,
    tmp_path: Path,
) -> None:
    """Недописанная последняя rollout-строка не входит в raw_record_count (P2-26)."""
    session_file = tmp_path / "torn_tail_rollout.jsonl"
    write_jsonl_file(
        session_file,
        [{"type": "event_msg", "payload": {"type": "task_complete"}}],
    )
    with session_file.open("a", encoding="utf-8") as file_handle:
        file_handle.write('{"type": "event_msg", "payload": {"ty')

    cursor = await backend.read_session_file_cursor(str(session_file))
    snapshot = await backend.read_session_file_snapshot(str(session_file))

    assert cursor.raw_record_count == 1
    assert snapshot.raw_record_count == 1
```

Run: `python -m pytest tests/test_claude_code_backend.py tests/test_codex_backend.py -v -k torn`
Expected: FAIL все 3 (счётчики считают полустроку; во втором тесте дописанная запись имеет `raw_record_index == 2` при baseline-курсоре `2` — фильтр `> 2` её исключает).

- [ ] **Step 5: Применить dropper в четырёх точках чтения**

В `src/claude_manager/claude_code_session_file_reader.py`:

импорт:

```python
from claude_manager.jsonl_incomplete_trailing_line_dropper import (
    drop_incomplete_trailing_jsonl_line,
)
```

в `_read_session_file_snapshot_blocking` после строки `raw_lines = _read_file_lines_blocking(file_path)` (строка 436):

```python
        raw_lines = drop_incomplete_trailing_jsonl_line(raw_lines)
```

в `_read_cursor_count_last_record_and_turn_active` заменить построчный сбор (строки 494-500):

```python
    non_empty_lines: list[str] = []
    with open(file_path, encoding="utf-8") as file_handle:
        raw_lines = drop_incomplete_trailing_jsonl_line(file_handle.readlines())
    for raw_line in raw_lines:
        stripped_line = raw_line.strip()
        if not stripped_line:
            continue
        non_empty_lines.append(stripped_line)
```

В `src/claude_manager/codex_session_file_reader.py`:

тот же импорт; в `_read_session_file_snapshot_blocking` после `raw_lines = _read_file_lines_blocking(file_path)` (строка 246):

```python
        raw_lines = drop_incomplete_trailing_jsonl_line(raw_lines)
```

в `_read_cursor_record_count_and_last_record` заменить построчный подсчёт (строки 296-304):

```python
    with open(file_path, encoding="utf-8") as file_handle:
        raw_lines = drop_incomplete_trailing_jsonl_line(file_handle.readlines())

    raw_record_count = 0
    last_raw_record = ""
    for raw_line in raw_lines:
        stripped_line = raw_line.strip()
        if not stripped_line:
            continue
        raw_record_count += 1
        last_raw_record = stripped_line
```

(остальное тело обеих функций — без изменений).

- [ ] **Step 6: Убедиться, что reproduce-тесты проходят**

Run: `python -m pytest tests/test_claude_code_backend.py tests/test_codex_backend.py tests/test_jsonl_incomplete_trailing_line_dropper.py -v`
Expected: PASS все.

- [ ] **Step 7: Полный прогон**

Run: `python -m pytest tests/ -v`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add src/claude_manager/jsonl_incomplete_trailing_line_dropper.py \
        src/claude_manager/claude_code_session_file_reader.py \
        src/claude_manager/codex_session_file_reader.py \
        tests/test_jsonl_incomplete_trailing_line_dropper.py \
        tests/test_claude_code_backend.py \
        tests/test_codex_backend.py
git commit -m "fix: читатели сессий не считают недописанную последнюю строку JSONL (P2-26)"
```

---

### Task 6: Финальная верификация этапа 1 и отметки в реестре

**Files:**
- Modify: `dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md`
- Modify: `dev/docs/specs/04.07_21.27-project-wide-bug-fix-roadmap-and-stage-1-cursor-plan.md` (этот файл — чекбоксы)

- [ ] **Step 1: Полный прогон всей сюиты**

Run: `source .venv/bin/activate && python -m pytest tests/ -v`
Expected: PASS все (921 существующий + ~15 новых).

- [ ] **Step 2: Отметить исправленное в реестре находок**

В `dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md` в заголовки P1-1, P1-2 и первую строку пункта P2-26 добавить в конец: ` — ✅ исправлено 04.07.2026 (этап 1 плана 04.07_21.27)`.

- [ ] **Step 3: Проставить галочки этапа 1 в роудмапе этого файла**

- [ ] **Step 4: Commit**

```bash
git add dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md \
        dev/docs/specs/04.07_21.27-project-wide-bug-fix-roadmap-and-stage-1-cursor-plan.md
git commit -m "docs: отметки об исправлении P1-1, P1-2, P2-26 в реестре багов и роудмапе"
```

- [ ] **Step 5: Перезапуск бота для выката фиксов**

Из терминала (НЕ из Bash-подпроцесса бота): `./restart-claude-manager.sh` — либо команда `/restart` в Telegram-боте. Проверить post-flight вывод скрипта.

---

## После этапа 1

Перед стартом этапа 2 (диагностика ошибок CLI) — написать детальный план этого кластера по актуальному коду тем же форматом и добавить его рядом в `dev/docs/specs/` (либо дописать в этот файл отдельной секцией). Ключевая идея этапа 2 из реестра: отдельный тип исключения «сбой старта CLI» против «busy» закрывает P2-15/P2-16 и упрощает P2-14/P2-17.
