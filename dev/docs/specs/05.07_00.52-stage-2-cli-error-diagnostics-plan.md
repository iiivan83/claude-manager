# Детальный план этапа 2: честная диагностика ошибок запуска CLI (P2-14, P2-15, P2-16, P2-17)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Закрыть кластер «диагностика ошибок CLI» из реестра `dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md` (P2-14, P2-15, P2-16, P2-17). Корень кластера — бот не отличает «сбой ЗАПУСКА CLI-процесса» от «процесс занят» и в обоих случаях шлёт пользователю «агент занят, подождите или /stop»; плюс резолюция алиасов устаревшего `session_id` для движка Claude не находит tuple-ключи, из-за чего `/stop`/busy-гейт промахиваются после temp→real ремапа.

**Architecture:** Одно новое исключение `CodingAgentStartError` (подкласс `ProcessManagerError`) отделяет «не смогли стартовать CLI» от «занят» и закрывает P2-15/P2-16. Починка `_resolve_process_key_alias_unlocked`, чтобы она находила и голые, и tuple-формы CLAUDE-ключей, закрывает P2-14; busy-гейт, резолвящий алиас перед проверкой занятости, закрывает P2-17. Изменения точечные, в инфраструктурном слое process_* и в двух местах транспортного слоя (`claude_interaction`, `reply_route_handler`).

**Tech Stack:** Python 3.13, pytest (asyncio_mode=auto), dataclasses. Юнит-тесты в `tests/test_process_manager.py` (существующий файл; автофикстура чистит `_processes`/`_busy_flags`/`_stop_events`/`_session_id_aliases`).

---

## Как работать по этому плану

- **Реестр деталей** — `dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md`: у каждого бага там файлы/строки, сценарий, вердикт верификации (см. строки P2-14…P2-17).
- **TDD всегда:** сначала тест, воспроизводящий баг (и падающий), затем минимальный фикс, затем полный прогон.
- **Полный прогон:** `source .venv/bin/activate && python -m pytest tests/ -v` (юнит + интеграционные; e2e не входят в обязательный прогон этапа).
- **Коммиты:** один тематический коммит на задачу, стейджить поимённо (`git add <путь>`), без `git add -A`. Работа прямо в `main` (глобальное правило Work In Main).
- **Отметки прогресса:** после фикса — галочка в чекбоксе роудмапа `04.07_21.27-...` (этап 2) И пометка «✅ исправлено DD.MM.YYYY» в реестре находок (см. Task 5).
- **Рестарт бота** после слияния фиксов — ТОЛЬКО из терминала `./restart-claude-manager.sh` или командой `/restart` в самом боте. Никогда из Bash-подпроцесса бота (принцип запрета самоперезапуска, инцидент 22-04-2026).
- **Размер файлов:** все правки этапа 2 — маленькие точечные добавления в уже существующие функции; ни один из тронутых файлов process_* не приближается к порогу 300 строк. Сообщать размер при правке не требуется (пороги не пробиваются), но проверить это фактически при каждой правке.

---

## Порядок и зависимости задач

- **Task 1 — P2-15** (новое исключение `CodingAgentStartError` + честные обработчики). Базовый: вводит тип, на который опираются P2-16 и обработчики.
- **Task 2 — P2-16** (`_restart_process` внутрь try в ретрае). Зависит от Task 1: транзиентный `CodingAgentStartError` при ретрае должен уводить в `continue`, а не рвать цикл.
- **Task 3 — P2-14** (резолюция алиасов находит tuple-ключи CLAUDE). Независим от Task 1/2.
- **Task 4 — P2-17** (busy-гейт резолвит алиас). Зависит от Task 3: без починки резолюции гейт не найдёт занятость по устаревшему id.
- **Task 5** — финальная верификация, отметки в реестре и роудмапе, рестарт бота.

Справка по ключам процессов (нужна в Task 3/4):

- `_make_process_key(sid, CLAUDE)` → голая строка `sid`; `_make_process_key(sid, CODEX)` → tuple `(sid, CODEX)`.
- `_make_backend_process_key(sid, backend)` → всегда tuple `(sid, backend)` (backend-aware путь всегда пишет состояние под tuple).
- `update_session_id` при tuple-`old_key` сохраняет tuple-форму: алиас CLAUDE-ремапа лежит под tuple `('_new_x', CLAUDE) → (real, CLAUDE)`.
- `_resolve_process_key_alias_unlocked(sid, CLAUDE)` стартует с голой строки `sid` → цикл `while resolved_key in _session_id_aliases` НИКОГДА не совпадает с tuple-ключом алиаса (это и есть баг P2-14).

---

### Task 1: P2-15 — исключение `CodingAgentStartError` отделяет «сбой старта CLI» от «занят»

**Files:**
- Modify: `src/claude_manager/process_types.py:44-46` (добавить класс)
- Modify: `src/claude_manager/process_lifecycle.py:30-34, 66-68, 96-99, 162-165` (raise нового типа)
- Modify: `src/claude_manager/process_manager.py:59-78` (ре-экспорт из фасада)
- Modify: `src/claude_manager/claude_interaction.py:632-655` (различать тип внутри обработчика)
- Modify: `src/claude_manager/reply_route_handler.py:271-285` (убрать string-match костыль)
- Test: `tests/test_process_manager.py` (новый тест + ужесточение `test_backend_start_error_cleans_control_state:395-414`)

Имя `CodingAgentStartError` утверждено пользователем 05.07.2026 (согласовано с существующим `ClaudeStartError` и доменным словом проекта `CodingAgentBackend`).

- [ ] **Step 1: Написать падающий тест**

В `tests/test_process_manager.py` в тестовый класс backend-aware отправки (рядом с `test_backend_start_error_cleans_control_state`) добавить тест, что сбой старта — это именно `CodingAgentStartError`, а busy — обычный `ProcessManagerError`:

```python
    async def test_backend_start_failure_raises_coding_agent_start_error(self) -> None:
        """Сбой старта CLI — CodingAgentStartError, а не голый ProcessManagerError (P2-15)."""
        session_id = "_new_start_fail"
        with mock.patch(
            "claude_manager.process_lifecycle.start_subprocess_for_backend",
            new=mock.AsyncMock(side_effect=OSError("binary gone")),
        ):
            with pytest.raises(CodingAgentStartError):
                await pm_module._send_message_backend_aware(
                    session_id,
                    "привет",
                    backend=BackendName.CODEX,
                    image_paths=None,
                    cwd="/tmp",
                    progress_callback=None,
                    retry_callback=None,
                    session_id_callback=None,
                )

    async def test_busy_rejection_is_not_coding_agent_start_error(self) -> None:
        """Занятость остаётся обычным ProcessManagerError, не подтипом старта (P2-15)."""
        session_id = "sess-busy"
        pm_module._busy_flags[(session_id, BackendName.CODEX)] = True
        with pytest.raises(ProcessManagerError) as exc_info:
            await pm_module._send_message_backend_aware(
                session_id,
                "привет",
                backend=BackendName.CODEX,
                image_paths=None,
                cwd="/tmp",
                progress_callback=None,
                retry_callback=None,
                session_id_callback=None,
            )
        assert not isinstance(exc_info.value, CodingAgentStartError)
```

Добавить `CodingAgentStartError` в импорт из `claude_manager.process_types` в шапке файла (блок строк 59-78 существующего импорта).

- [ ] **Step 2: Убедиться, что тест падает**

Run: `python -m pytest tests/test_process_manager.py -v -k "coding_agent_start_error or not_coding_agent_start_error"`
Expected: FAIL — `ImportError: cannot import name 'CodingAgentStartError'` (класс ещё не существует).

- [ ] **Step 3: Ввести исключение и переключить raise-сайты**

В `src/claude_manager/process_types.py` после `class ProcessManagerError` (строка 44-46) добавить:

```python
class CodingAgentStartError(ProcessManagerError):
    """Не удалось ЗАПУСТИТЬ CLI-процесс агента (Claude/Codex) — в отличие от «занят».

    Подкласс ProcessManagerError: существующие широкие `except ProcessManagerError`
    продолжают ловить сбой старта (обратная совместимость), но обработчик выше по
    стеку может отличить «CLI не стартовал» от «процесс уже обрабатывает прошлый
    запрос» и показать честное сообщение вместо ложного «занят, подождите».
    """
```

В `src/claude_manager/process_lifecycle.py`:
- в импорте из `claude_manager.process_types` (строки 30-34) добавить `CodingAgentStartError`;
- в `_start_subprocess_for_backend_turn` (строки 66-68) заменить `raise ProcessManagerError(` на `raise CodingAgentStartError(`;
- в `_restart_process`, Claude-ветка (строки 96-99), заменить `raise ProcessManagerError(` на `raise CodingAgentStartError(`;
- в `create_process` (строки 162-165) заменить `raise ProcessManagerError(` на `raise CodingAgentStartError(`.

(Тексты сообщений оставить как есть: `f"Не удалось запустить CLI: {error}"` и `f"Не удалось запустить Claude: {error}"`.)

В `src/claude_manager/process_manager.py` добавить `CodingAgentStartError` в импорт из `claude_manager.process_types` (блок строк 59-78), чтобы был доступен `process_manager.CodingAgentStartError`.

- [ ] **Step 4: Различить тип в обработчике claude_interaction**

В `src/claude_manager/claude_interaction.py` в обработчике `except process_manager.ProcessManagerError as error:` (строки 632-655) сохранить общую логику восстановления anchor (строки 633-647 без изменений), но выбрать сообщение и уровень лога по типу ошибки. Заменить блок логирования+отправки (строки 648-655) на:

```python
        if isinstance(error, process_manager.CodingAgentStartError):
            logger.error(
                "Не удалось запустить CLI-агента (chat_id=%d): %s", chat_id, error,
                exc_info=True,
            )
            user_message = (
                f"Не удалось запустить {_get_backend_display_name(backend)}. "
                "Проверьте, что CLI установлен, и попробуйте ещё раз"
            )
        else:
            logger.warning("Процесс занят (chat_id=%d): %s", chat_id, error)
            user_message = (
                f"{_get_backend_display_name(backend)} ещё обрабатывает "
                "предыдущее сообщение. Подождите или /stop"
            )
        await _get_send_telegram_message()(chat_id, user_message, parse_mode=None)
```

Почему в общем обработчике через `isinstance`, а не отдельным `except` выше: восстановление turn-anchor (строки 633-647) одинаково нужно и при busy, и при сбое старта (ход не состоялся — anchor не должен «прилипнуть»); дублировать этот блок в двух `except` — это дублирование логики (запрет из «Правила чистого кода»).

- [ ] **Step 5: Убрать string-match костыль в reply_route_handler**

В `src/claude_manager/reply_route_handler.py` заменить единый `except process_manager.ProcessManagerError` (строки 271-285) на два обработчика — специфичный подтип ПЕРЕД базовым (порядок обязателен, иначе подтип перехватит базовый `except`):

```python
        except process_manager.CodingAgentStartError as error:
            logger.warning("Reply-route send failed to start CLI: %s", error)
            await _send_route_error(context, chat_id, link, "не удалось запустить агент")
            return
        except process_manager.ProcessManagerError as error:
            logger.warning("Reply-route send rejected as busy: %s", error)
            await _send_route_error(
                context, chat_id, link, "сессия занята. Подождите или /stop",
            )
            return
```

Это удаляет прежний `error_text`-string-match (`"busy" in error_text or "занят" in error_text`) — он был именно костылём под отсутствие типа (подтверждение реальности P2-15 в реестре). Проверить, что `is_busy`-предпроверка в `_target_is_busy`/`send_key` (строки 51-55, 322-327) остаётся без изменений — она отсекает занятость ДО отправки и к этому обработчику не относится.

- [ ] **Step 6: Ужесточить существующий тест старта**

В `tests/test_process_manager.py:395-414` (`test_backend_start_error_cleans_control_state`) заменить `pytest.raises(ProcessManagerError, match="Не удалось запустить CLI")` на `pytest.raises(CodingAgentStartError, match="Не удалось запустить CLI")` — контракт нового типа фиксируется тестом. Логика очистки control-state (busy/stop) не меняется.

- [ ] **Step 7: Прогон новых и затронутых тестов**

Run: `python -m pytest tests/test_process_manager.py -v -k "start_error or busy or coding_agent"`
Expected: PASS все.

- [ ] **Step 8: Полный прогон**

Run: `python -m pytest tests/ -v`
Expected: PASS. Если падают тесты `tests/test_claude_interaction.py` или `tests/test_reply_route_handler.py`, ожидавшие busy-текст на сбое старта — это и есть починенное поведение: обновить ожидания на новый текст «Не удалось запустить …»/«не удалось запустить агент». Семантику не ослаблять.

- [ ] **Step 9: Commit**

```bash
git add src/claude_manager/process_types.py \
        src/claude_manager/process_lifecycle.py \
        src/claude_manager/process_manager.py \
        src/claude_manager/claude_interaction.py \
        src/claude_manager/reply_route_handler.py \
        tests/test_process_manager.py
git commit -m "fix: CodingAgentStartError отделяет сбой старта CLI от busy (P2-15)"
```

(Плюс тестовые файлы транспортного слоя, если правились на Step 8, — добавить поимённо.)

---

### Task 2: P2-16 — `_restart_process` внутри try, транзиентный сбой старта не рвёт retry-цикл

**Files:**
- Modify: `src/claude_manager/process_retry.py:55-91`
- Test: `tests/test_process_manager.py`

- [ ] **Step 1: Написать падающие тесты**

В `tests/test_process_manager.py` добавить два теста поведения `_execute_single_retry`:

```python
    async def test_execute_single_retry_returns_none_on_transient_start_failure(self) -> None:
        """Транзиентный сбой старта в ретрае → None (продолжаем цикл), не пробрасываем (P2-16)."""
        with mock.patch(
            "claude_manager.process_retry._restart_process",
            new=mock.AsyncMock(side_effect=CodingAgentStartError("spawn flaked")),
        ):
            result = await _execute_single_retry(
                "sess-retry",
                "текст",
                attempt=1,
                cwd="/tmp",
                progress_callback=None,
                backend_obj=object(),
                backend_name=BackendName.CODEX,
            )
        assert result is None

    async def test_execute_single_retry_propagates_stop(self) -> None:
        """Сбой рестарта из-за /stop обязан пробрасываться, а не гаситься в None (P2-16)."""
        with mock.patch(
            "claude_manager.process_retry._restart_process",
            new=mock.AsyncMock(side_effect=ProcessStoppedError("stopped")),
        ):
            with pytest.raises(ProcessStoppedError):
                await _execute_single_retry(
                    "sess-retry",
                    "текст",
                    attempt=1,
                    cwd="/tmp",
                    progress_callback=None,
                    backend_obj=object(),
                    backend_name=BackendName.CODEX,
                )
```

Импортировать `_execute_single_retry` и `CodingAgentStartError` в шапке (первый уже есть в импорте из `process_retry`; проверить).

- [ ] **Step 2: Убедиться, что первый тест падает**

Run: `python -m pytest tests/test_process_manager.py -v -k "single_retry_returns_none or single_retry_propagates_stop"`
Expected: FAIL `test_execute_single_retry_returns_none_on_transient_start_failure` — `CodingAgentStartError` вылетает из `_execute_single_retry` наружу (рестарт вне try); PASS `test_execute_single_retry_propagates_stop` (ProcessStoppedError и сейчас пробрасывается, т.к. рестарт вне try).

- [ ] **Step 3: Реализация — перенести рестарт внутрь try**

В `src/claude_manager/process_retry.py` в `_execute_single_retry` (строки 55-91) внести старый-процесс-kill и `_restart_process` ВНУТРЬ существующего `try` (строка 72). Результат:

```python
    process_key = (
        _make_backend_process_key(session_id, backend_name)
        if backend_obj is not None
        else _prefer_existing_process_key_unlocked(session_id, backend_name)
    )
    try:
        old_process = _processes.get(process_key)
        if old_process is not None and old_process.is_running():
            await _apply_backend_stop_strategy(old_process, backend_name)
        claude_process = await _restart_process(
            session_id,
            cwd,
            backend_obj=backend_obj,
            backend_name=backend_name,
            prompt_text=text,
            image_paths=image_paths or [],
        )
        if isinstance(claude_process, ClaudeProcess):
            await claude_process.send_message(text)
        return await _process_events(
            claude_process,
            session_id,
            progress_callback,
            session_id_callback,
            backend_obj=backend_obj,
            backend_name=backend_name,
            session_id_callback_includes_backend=session_id_callback_includes_backend,
        )
    except ProcessStoppedError:
        raise
    except Exception:
        logger.warning(
            "Ошибка при повторной попытке %d для сессии %s",
            attempt, session_id, exc_info=True,
        )
        return None
```

Порядок `except` сохранить: `ProcessStoppedError` ловится и пробрасывается ПЕРВЫМ (иначе `except Exception` проглотит /stop). `CodingAgentStartError`/`OSError`/прочие транзиентные сбои старта → `except Exception` → `return None` → `_retry_loop` делает `continue` на следующую попытку.

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `python -m pytest tests/test_process_manager.py -v -k "single_retry"`
Expected: PASS оба.

- [ ] **Step 5: Полный прогон**

Run: `python -m pytest tests/ -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/claude_manager/process_retry.py tests/test_process_manager.py
git commit -m "fix: транзиентный сбой рестарта в ретрае не рвёт весь retry-цикл (P2-16)"
```

---

### Task 3: P2-14 — резолюция алиасов находит tuple-ключи CLAUDE-ремапов

**Files:**
- Modify: `src/claude_manager/process_state.py:54-69` (+ новый хелпер перед `_resolve_process_key_alias_unlocked`)
- Modify: `src/claude_manager/process_manager.py` (ре-экспорт хелпера — по образцу существующих `_resolve_*`)
- Test: `tests/test_process_manager.py`

Имя нового приватного хелпера — `_find_registered_alias_key` (находит форму ключа, под которой алиас реально зарегистрирован в `_session_id_aliases`).

- [ ] **Step 1: Написать падающие тесты**

В `tests/test_process_manager.py` добавить (используют автофикстуру очистки словарей):

```python
    async def test_stale_claude_temp_id_resolves_after_remap(self) -> None:
        """После CLAUDE temp→real ремапа устаревший temp-id резолвится в real (P2-14)."""
        real_id = "real-uuid"
        pm_module._session_id_aliases[("_new_claude", BackendName.CLAUDE)] = (
            real_id, BackendName.CLAUDE,
        )
        resolved = pm_module._resolve_process_key_alias_unlocked(
            "_new_claude", BackendName.CLAUDE,
        )
        assert resolved == (real_id, BackendName.CLAUDE)

    async def test_is_busy_and_has_process_follow_claude_tuple_alias(self) -> None:
        """/stop, is_busy, has_process находят процесс по устаревшему CLAUDE temp-id (P2-14)."""
        real_id = "real-uuid-2"
        running_process = mock.Mock()
        running_process.is_running.return_value = True
        pm_module._processes[(real_id, BackendName.CLAUDE)] = running_process
        pm_module._busy_flags[(real_id, BackendName.CLAUDE)] = True
        pm_module._session_id_aliases[("_new_x", BackendName.CLAUDE)] = (
            real_id, BackendName.CLAUDE,
        )

        assert is_busy("_new_x", BackendName.CLAUDE) is True
        assert has_process("_new_x", BackendName.CLAUDE) is True
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `python -m pytest tests/test_process_manager.py -v -k "stale_claude_temp_id or follow_claude_tuple_alias"`
Expected: FAIL оба — резолюция стартует с голой строки `_new_claude`, а алиас лежит под tuple `('_new_claude', CLAUDE)`, цикл не совпадает; `is_busy`/`has_process` возвращают False.

- [ ] **Step 3: Реализация**

В `src/claude_manager/process_state.py` перед `_resolve_process_key_alias_unlocked` (строка 54) добавить хелпер:

```python
def _find_registered_alias_key(process_key: ProcessKey) -> ProcessKey | None:
    """Находит ключ в _session_id_aliases с учётом string/tuple-форм CLAUDE."""
    if process_key in _session_id_aliases:
        return process_key
    session_id, backend = _split_process_key(process_key)
    if backend != BackendName.CLAUDE:
        return None
    # CLAUDE-состояние живёт под голой строкой ИЛИ под tuple (sid, CLAUDE) —
    # это одна сессия. Алиас backend-aware ремапа записан под tuple, а вызов
    # мог прийти с голой строкой (и наоборот): пробуем альтернативную форму.
    alternate_key: ProcessKey = (
        (session_id, BackendName.CLAUDE) if isinstance(process_key, str) else session_id
    )
    if alternate_key in _session_id_aliases:
        return alternate_key
    return None
```

и переписать `_resolve_process_key_alias_unlocked` (строки 54-69), чтобы она искала алиас через хелпер и детектила цикл по посещённым ключам:

```python
def _resolve_process_key_alias_unlocked(
    session_id: str,
    backend: BackendName = BackendName.CLAUDE,
) -> ProcessKey:
    """Возвращает актуальный process key по цепочке алиасов."""
    resolved_key = _make_process_key(session_id, backend)
    seen: set[ProcessKey] = set()

    while resolved_key not in seen:
        seen.add(resolved_key)
        alias_source_key = _find_registered_alias_key(resolved_key)
        if alias_source_key is None:
            return resolved_key
        resolved_key = _session_id_aliases[alias_source_key]

    logger.error("Обнаружен цикл алиасов process key: %s", resolved_key)
    return resolved_key
```

В `src/claude_manager/process_manager.py` добавить `_find_registered_alias_key` в импорт из `claude_manager.process_state` (рядом с `_resolve_process_key_alias_unlocked`, строки 7-25) — фасад ре-экспортирует внутренние хелперы, тест `test_process_manager_reexports_process_state_objects` это проверяет.

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `python -m pytest tests/test_process_manager.py -v -k "stale_claude_temp_id or follow_claude_tuple_alias or reexport"`
Expected: PASS все.

- [ ] **Step 5: Полный прогон**

Run: `python -m pytest tests/ -v`
Expected: PASS. Убедиться, что CODEX-путь резолюции (tuple с самого начала) не сломан — существующие тесты `test_update_session_id_preserves_backend_key`, `test_stop_process_uses_codex_stop_strategy` должны проходить без правок.

- [ ] **Step 6: Commit**

```bash
git add src/claude_manager/process_state.py \
        src/claude_manager/process_manager.py \
        tests/test_process_manager.py
git commit -m "fix: резолюция алиасов находит tuple-ключи CLAUDE-ремапов (P2-14)"
```

---

### Task 4: P2-17 — busy-гейт резолвит алиас перед проверкой занятости

**Files:**
- Modify: `src/claude_manager/process_backend_send.py:19-25 (импорт), 80-86 (гейт)`
- Test: `tests/test_process_manager.py`

- [ ] **Step 1: Написать падающий тест**

В `tests/test_process_manager.py` добавить:

```python
    async def test_busy_gate_rejects_stale_id_after_remap(self) -> None:
        """Отправка со stale temp-id видит занятость real-сессии и отклоняется (P2-17)."""
        real_id = "real-uuid-3"
        pm_module._busy_flags[(real_id, BackendName.CLAUDE)] = True
        pm_module._session_id_aliases[("_new_stale", BackendName.CLAUDE)] = (
            real_id, BackendName.CLAUDE,
        )
        with pytest.raises(ProcessManagerError, match="занят"):
            await pm_module._send_message_backend_aware(
                "_new_stale",
                "второй ход",
                backend=BackendName.CLAUDE,
                image_paths=None,
                cwd="/tmp",
                progress_callback=None,
                retry_callback=None,
                session_id_callback=None,
            )
        # Занятость real-сессии не затёрта: гейт отклонил ход ДО try/finally.
        assert pm_module._busy_flags[(real_id, BackendName.CLAUDE)] is True
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `python -m pytest tests/test_process_manager.py -v -k "busy_gate_rejects_stale_id"`
Expected: FAIL — гейт читает `_busy_flags[('_new_stale', CLAUDE)]` (False), не резолвя алиас, и уходит в `_restart_process` (реальный старт CLI не замокан) вместо отказа «занят».

- [ ] **Step 3: Реализация**

В `src/claude_manager/process_backend_send.py` в импорт из `claude_manager.process_state` (строки 19-25) добавить `_resolve_process_key_alias_unlocked` и `_split_process_key`.

Заменить busy-гейт (строки 80-86) на резолвящий алиас перед проверкой:

```python
    process_key = _make_backend_process_key(session_id, effective_backend)
    owner_stop_event = asyncio.Event()
    async with _busy_lock:
        # Резолвим алиас: после temp→real ремапа busy-флаг лежит под real-id,
        # а вызов мог прийти с устаревшим temp-id. Без резолюции гейт увидит
        # busy=False и стартует второй параллельный ход (P2-17). Канонический
        # ключ строится тем же _make_backend_process_key, что и все SET-ключи.
        resolved_session_id, _resolved_backend = _split_process_key(
            _resolve_process_key_alias_unlocked(session_id, effective_backend)
        )
        busy_check_key = _make_backend_process_key(
            resolved_session_id, effective_backend,
        )
        if _busy_flags.get(busy_check_key, False):
            raise ProcessManagerError(f"Процесс {busy_check_key} уже занят")
        _busy_flags[process_key] = True
        _stop_events[process_key] = owner_stop_event
```

Флаг/стоп-событие владельца по-прежнему ставятся под сырой `process_key` (идентичность владельца для finally-очистки), проверка занятости — под резолвленным `busy_check_key`. Для нового id без алиаса `busy_check_key == process_key` — поведение без изменений.

- [ ] **Step 4: Убедиться, что тест проходит**

Run: `python -m pytest tests/test_process_manager.py -v -k "busy_gate_rejects_stale_id"`
Expected: PASS.

- [ ] **Step 5: Полный прогон**

Run: `python -m pytest tests/ -v`
Expected: PASS. Проверить, что нормальная отправка нового id и повторная отправка того же занятого id (существующие тесты busy) не сломаны.

- [ ] **Step 6: Commit**

```bash
git add src/claude_manager/process_backend_send.py tests/test_process_manager.py
git commit -m "fix: busy-гейт резолвит алиас устаревшего session_id (P2-17)"
```

---

### Task 5: Финальная верификация этапа 2 и отметки в реестре

**Files:**
- Modify: `dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md`
- Modify: `dev/docs/specs/04.07_21.27-project-wide-bug-fix-roadmap-and-stage-1-cursor-plan.md` (чекбоксы этапа 2)

- [ ] **Step 1: Полный прогон всей сюиты**

Run: `source .venv/bin/activate && python -m pytest tests/ -v`
Expected: PASS все (существующие + ~7 новых тестов этапа 2).

- [ ] **Step 2: Отметить исправленное в реестре находок**

В `dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md` в заголовки P2-14, P2-15, P2-16, P2-17 добавить в конец: ` — ✅ исправлено DD.MM.YYYY (этап 2 плана 05.07_00.52)`.

- [ ] **Step 3: Проставить галочки этапа 2 в роудмапе `04.07_21.27-...`**

Отметить `[x]` для P2-15, P2-16, P2-14, P2-17 в секции «Этап 2».

- [ ] **Step 4: Commit**

```bash
git add dev/docs/specs/04.07_13.12-project-wide-bug-review-findings.md \
        dev/docs/specs/04.07_21.27-project-wide-bug-fix-roadmap-and-stage-1-cursor-plan.md
git commit -m "docs: отметки об исправлении P2-14..P2-17 в реестре багов и роудмапе"
```

- [ ] **Step 5: Перезапуск бота для выката фиксов**

Из терминала (НЕ из Bash-подпроцесса бота): `./restart-claude-manager.sh` — либо команда `/restart` в Telegram-боте. Проверить post-flight вывод скрипта.

---

## Что НЕ входит в этот план (отложено)

Пункты «по касанию» этапа 2 из роудмапа (P3-21…P3-30) сознательно вынесены за рамки этой сессии по решению пользователя (05.07.2026: «Только ядро P2»). Они чинятся отдельным заходом, когда касание тех же файлов станет естественным, либо явно фиксируются как «известный латентный дефект» в реестре. Список: P3-21 (мёртвый fallback `_check_stop_requested` для CLAUDE), P3-22 (`session_id_callback` только для первой смены id), P3-23 (сломанный legacy-путь отправки), P3-24 (`create_process` под строковыми ключами), P3-25 (мёртвые записи в `_processes`), P3-26 (гонка `send_signal` с завершением), P3-27 (temp-id `_new_*` в дневном реестре), P3-28 (одноразовый `ClaudeProcess` в legacy-переиспользовании), P3-29 (строка stdout >16 МБ / обрыв UTF-8), P3-30 (утечка сабпроцесса при падении первой записи в stdin).
