---
name: root-cause-analysis
description: >-
  Универсальный глубокий анализ и поиск первопричины проблемы в любом проекте.
  При запуске самостоятельно изучает структуру проекта (CLAUDE.md, файлы, код,
  скиллы), читает глобальные референсы из ~/.claude/references/, обнаруживает
  все скиллы проекта и строит цепочку причин от симптома до корневой причины.
  Проводит двойную верификацию выводов с самостоятельной проверкой рекомендаций
  на соответствие архитектуре и стандартам проекта. Создаёт подробный отчёт
  с категоризированным чек-листом исправлений (DOC/CODE/SKILL).
  Используй когда пользователь хочет найти первопричину проблемы, разобраться
  почему что-то сломалось, провести глубокий анализ бага, говорит «найди причину»,
  «почему сломалось», «root cause», «первопричина», «разберись в проблеме»,
  «проанализируй баг», «почему не работает», «что пошло не так», «deep analysis»,
  «глубокий анализ проблемы», «расследуй баг», «откуда ошибка», «в чём корень
  проблемы». Также используй если пользователь описывает баг и хочет понять его
  природу, а не просто исправить.
user_invocable: true
---

# Глубокий анализ первопричины проблемы

## Global Reference Rules

Перед началом работы прочитай `~/.claude/references/writing-style-guide.md` — единый стиль письма. Этот скилл дополняет общий стиль своими требованиями к формату отчёта в Фазе 7.

Перед ответом включи максимально глубокий режим размышлений (extended thinking).

## Глубина мышления

Думай как опытный тех-лид, который лично отвечает за стабильность продакшена. Каждое решение
проходит через внутренний чек-лист:

- **Побочные эффекты** — что может сломаться в соседних модулях из-за этого изменения?
- **Граничные случаи** — как это поведёт себя на пустых данных, при конкурентном доступе,
  при неожиданном порядке вызовов?
- **Откат** — если изменение окажется неудачным, как быстро можно вернуть всё назад?
- **Альтернативы** — есть ли более простой способ добиться того же результата?
- **Достаточность** — это изменение действительно решает корневую причину, а не маскирует
  симптом?

Все агенты этого скилла работают с такой же глубиной — разбирают задачу по частям,
рассматривают альтернативы и перепроверяют выводы перед тем, как действовать.

Ты — следователь-аналитик. Твоя задача — докопаться до самой глубокой, настоящей
причины проблемы. Не остановиться на первом подозрительном месте, а пройти всю
цепочку до конца. Потом проверить себя и пройти ещё раз.

Аргументы пользователя: $ARGUMENTS

## Принцип: копай до дна

Поверхностная причина — это не ответ. Если ошибка в том, что пакет не установлен —
нужно понять, почему его не установили. Если спецификация пропустила зависимость —
нужно понять, почему скилл, создававший спецификацию, не проверил зависимости.
Каждый ответ «потому что X» порождает следующий вопрос «а почему X?».

Останавливайся только когда доходишь до:
- Пробела в процессе (скилл/документация/чеклист не покрывает этот случай)
- Человеческого решения (кто-то сознательно решил не делать X)
- Системного ограничения (технология не позволяет)

---

## Фаза 0: Разведка проекта

Эта фаза обязательна при каждом запуске. Ты начинаешь как новый человек в проекте —
ничего не знаешь заранее, всё узнаёшь из файлов.

### 0.1 Прочитай CLAUDE.md проекта

Найди и прочитай CLAUDE.md в корне проекта. Это главный документ проекта — из него
ты узнаешь:
- Что это за проект и что он делает
- Архитектурные принципы и слои
- Структуру папок (где код, тесты, документация, спецификации)
- Правила разработки и стандарты кода
- Команды для запуска тестов и сборки
- Важные детали и «подводные камни»

Запиши для себя **карту проекта**:
- **Исходный код:** [путь к папке с кодом]
- **Тесты:** [путь к тестам]
- **Документация:** [путь к документации]
- **Спецификации:** [путь к спецификациям]
- **Конфигурация:** [ключевые файлы конфигурации]

### 0.2 Прочитай глобальные референсы

Прочитай все файлы из `~/.claude/references/`:
- Эти документы описывают глобальные стандарты — как должны быть устроены документы,
  какие документы обязательны, как именовать файлы
- Используй эти стандарты при анализе: соблюдены ли они в проекте?
- Используй их при создании отчёта: размещай и именуй файлы по стандарту

### 0.3 Изучи структуру проекта

Используй `ls` и `glob` для исследования:
1. Корень проекта — какие папки и файлы есть
2. Папка с исходным кодом — какие модули/файлы
3. Папка с тестами — какие типы тестов (юнит, интеграционные, e2e)
4. Папка с документацией — какие документы, есть ли индекс
5. Конфигурационные файлы — pyproject.toml, package.json и т.д.

### 0.4 Обнаружь все скиллы проекта

Найди все скиллы в `.claude/skills/`:
```
glob .claude/skills/*/SKILL.md
```

Для каждого найденного скилла прочитай как минимум первые 20 строк (описание и
назначение). Составь **карту скиллов**:
- **Имя скилла** — что он делает (одно предложение)

Эта карта понадобится в Фазе 3 — для определения, какой скилл мог пропустить проблему.

### 0.5 Найди индексы документации

Поищи индексные файлы документации:
- Файлы с именами типа `docs-index`, `README`, `INDEX` в папке документации
- Если индекс есть — прочитай его, это карта всех документов проекта
- Если индекса нет — составь список документов через glob

### 0.6 Запиши результат разведки

Перед переходом к следующей фазе у тебя должна быть полная карта:
- Что за проект, его назначение и архитектура
- Где лежит код, тесты, документация, спецификации
- Какие скиллы есть и за что отвечают
- Какие глобальные стандарты действуют
- Какие документы проекта существуют

---

## Фаза 1: Сбор контекста проблемы

### 1.1 Пойми проблему

Прочитай описание проблемы от пользователя. Если пользователь дал путь к отчёту —
прочитай этот отчёт целиком. Если описал текстом — зафиксируй:
- Что именно сломалось (симптом)?
- Когда это проявляется?
- Какой ожидаемый результат vs фактический?

### 1.2 Найди связанные отчёты и логи

Используя карту проекта из Фазы 0, найди все отчёты и логи, которые упоминают
ключевые слова проблемы. Ищи через Grep в папках с документацией, отчётами сессий,
логами пайплайнов.

Часто проблема обсуждалась в нескольких сессиях, и в ранних сессиях могут быть
подсказки о корневой причине.

Прочитай ВСЕ найденные отчёты — не только последний.

---

## Фаза 2: Глубокий анализ документации

Используя карту документов из Фазы 0, определи какие документы могут быть связаны
с проблемой.

Обязательно проверь (если они существуют в проекте):

- **Спецификации модулей** — если проблема связана с конкретным модулем, найди
  его спецификацию. Проверь: описана ли в спеке работа с тем, что сломалось?

- **BRD / требования** — проверь ожидаемое поведение по описанию сценария.

- **CLAUDE.md** — главный документ проекта: не нарушены ли архитектурные решения?

- **Документы развёртывания** — если проблема связана с инфраструктурой запуска.

- **Состояние пайплайна** (pipeline-state.json или аналог) — если проект
  использует пайплайн, проверь на какой фазе что-то пошло не так.

- **Отчёты ревью кода** — содержат уже найденные и исправленные проблемы.
  Возможно, текущая проблема связана с тем, что ревью пропустило или
  исправило не до конца.

- **Результаты тестирования** — если проблема не была поймана тестами, важно
  понять почему: тест-план неполный, сценарий пропущен, или тест был зелёный,
  но проверял не то.

Для каждого прочитанного документа запиши: что нашёл и что НЕ нашёл (отсутствие
информации тоже важно).

---

## Фаза 3: Анализ скиллов

Это критически важная фаза. Проблемы часто возникают из-за того, что скилл,
создававший код или документацию, пропустил какой-то аспект.

### 3.1 Определи связанные скиллы

Используя карту скиллов из Фазы 0, подумай: какие скиллы участвовали в создании
кода/документации, где возникла проблема?

Типичные категории скиллов для проверки:
- Скиллы, создающие код (реализация модулей, генерация кода)
- Скиллы, создающие спецификации (проектирование, декомпозиция)
- Скиллы, проверяющие код (ревью, тестирование, валидация)
- Скиллы, управляющие процессом (оркестрация, пайплайны)
- Скиллы, создающие документацию

### 3.2 Прочитай каждый связанный скилл

Для каждого скилла из `.claude/skills/{skill-name}/SKILL.md`:

1. Прочитай SKILL.md **целиком**
2. Проверь: есть ли в скилле пункт, который должен был предотвратить эту проблему?
3. Если пункт есть — почему он не сработал? (неточная формулировка? пропущен случай?)
4. Если пункта нет — это пробел в скилле, который нужно зафиксировать

### 3.3 Запиши результат по каждому скиллу

Для отчёта запиши в формате:
- `skill-name` — есть/нет пункт про [что именно]. Вывод: [скилл пропустил X / скилл корректен / формулировка неточна]

---

## Фаза 4: Анализ кода

Используя карту проекта из Фазы 0, найди и прочитай код, связанный с проблемой.

### 4.1 Найди точное место ошибки

- Используй grep/glob чтобы найти файлы по ключевым словам из описания проблемы
- Прочитай каждый найденный файл целиком — не только строку с ошибкой, а весь контекст
- Проследи вызовы: кто вызывает проблемный код? Что вызывает он?

### 4.2 Сравни код со спецификацией

Если проблема в модуле, для которого есть спецификация:
- Найди спецификацию модуля в папке спецификаций (обнаруженной в Фазе 0)
- Прочитай спецификацию модуля
- Сравни: что описано в спеке, но не реализовано в коде? Что реализовано не так?
- Расхождение спеки и кода = потенциальная корневая причина

### 4.3 Проверь инфраструктуру

Если проблема может быть инфраструктурной, проверь конфигурационные файлы,
скрипты запуска и развёртывания, которые были обнаружены в Фазе 0.

### 4.4 Проверь тестовую инфраструктуру

Если проблема проявляется в тестах или могла быть поймана тестами:
- Общие фикстуры (conftest, helpers) — неправильная фикстура может маскировать баги
- Интеграционные тесты — если баг межмодульный, здесь может быть подсказка
- Юнит-тесты — проверяют ли они тот аспект, который сломался

---

## Фаза 5: Построение цепочки причин

Теперь собери всё в цепочку. Начни с симптома и задавай вопрос «почему?» на каждом
шаге:

```
Симптом: [что видит пользователь]
  ↓ потому что
Причина 1: [что именно сломалось в коде]
  ↓ потому что
Причина 2: [почему это оказалось сломано]
  ↓ потому что
Причина 3: [что привело к этому]
  ↓ потому что
КОРНЕВАЯ ПРИЧИНА: [самая глубокая причина — пробел в процессе/решение/ограничение]
```

Цепочка должна содержать минимум 3 звена. Если меньше — ты не докопался до дна.

---

## Фаза 6: ВЕРИФИКАЦИЯ (обязательная)

После того как ты построил цепочку причин и нашёл корневую причину — ОСТАНОВИСЬ
и пройди весь цикл заново. Это не формальность, а защита от ошибок.

### 6.1 Перечитай ключевые документы

Вернись к самым важным документам и перечитай их свежим взглядом. Теперь, когда у тебя
есть гипотеза, ты можешь заметить то, что пропустил в первый раз.

### 6.2 Проверь выводы на противоречия

- Объясняет ли найденная корневая причина ВСЕ симптомы? Если нет — причина не корневая
- Нет ли фактов, которые противоречат твоей теории?
- Нет ли более простого объяснения?

### 6.3 Рассмотри альтернативные объяснения

Придумай минимум 2 альтернативные версии и объясни, почему они менее вероятны.
Это заставляет тебя критически оценить свою основную версию.

### 6.4 Запиши результат верификации

Для отчёта запиши:
- Что перепроверил
- Какие альтернативы рассмотрел и почему отверг
- Подтвердилась ли корневая причина или изменилась

---

## Фаза 6.5: Самостоятельная верификация решений

После двойной проверки выводов (Фаза 6) и перед формированием финального отчёта —
предварительные рекомендации проходят самостоятельную верификацию. Это нужно, потому что
анализатор первопричин может предложить решения, которые выглядят разумно, но
противоречат архитектуре проекта или являются перестраховкой. Критический взгляд
помогает отфильтровать лишнее до того, как рекомендации попадут в финальный отчёт.

### 6.5.1 Подготовка предварительных рекомендаций

Сформируй черновой список всех рекомендаций по исправлению и предотвращению.
Каждая рекомендация должна содержать:
- Описание: что именно сделать
- Тег: `[ARCHITECTURE]`, `[DOC]`, `[CODE]` или `[SKILL]`
- Затрагиваемые файлы
- Обоснование: зачем это нужно

### 6.5.2 Верификация каждой рекомендации

Проверь каждую рекомендацию по 5 критериям (на основе CLAUDE.md и архитектуры
проекта, изученных в Фазе 0):
- **Архитектурное соответствие** — не нарушает ли принципы из CLAUDE.md
- **Соответствие требованиям** — не противоречит ли BRD или другим требованиям проекта
- **Кодовые стандарты** — соответствует ли правилам кодирования из CLAUDE.md
- **Необходимость** — решает ли это реальную проблему, или это перестраховка
- **Осуществимость** — можно ли это сделать сейчас, нет ли блокирующих зависимостей

Для каждой рекомендации вынеси вердикт:
- **ОДОБРЕНО** — рекомендация проходит все проверки
- **ОДОБРЕНО С ЗАМЕЧАНИЯМИ** — в целом годится, но нуждается в корректировке
- **ОТКЛОНЕНО** — перестраховка, не соответствует архитектуре или невыполнимо

### 6.5.3 Фильтрация

1. **ОДОБРЕНО** → рекомендация идёт в финальный отчёт без изменений
2. **ОДОБРЕНО С ЗАМЕЧАНИЯМИ** → скорректировать рекомендацию по замечаниям
3. **ОТКЛОНЕНО** → убрать из финального отчёта или заменить альтернативным решением

### 6.5.4 Фиксация результатов верификации

Для каждой рекомендации запиши:
- Вердикт
- Обоснование вердикта
- Была ли корректировка и какая

Эта информация войдёт в секцию «Верификация решений» финального отчёта.

---

## Фаза 7: Создание отчёта

Отчёт читает живой человек — владелец проекта. Поэтому отчёт пишется понятным
языком, адаптированным к стилю общения, принятому в проекте (см. CLAUDE.md).

### Стиль отчёта

**Язык:** Разговорный, дружелюбный. Никакого канцелярита и «птичьего языка».
Вместо «Обнаружен дефект маршрутизации запросов» — «Оказалось, что код отправлял
запрос, но не дожидался ответа — процесс падал на полпути».

**Сущности кода:** При первом упоминании любой сущности (класс, метод, файл,
команда, настройка) поясни что это и за что отвечает. При повторном
упоминании в том же отчёте пояснение не нужно.

**Технические термины:** Сначала понятное объяснение, потом техническое название
в скобках.

**Без таблиц:** Не используй markdown-таблицы. Вместо таблиц — буллеты
с **жирными** метками.

**Структура объяснения:** В каждом разделе придерживайся логики:
- Что проверялось / делалось — простыми словами
- Что пошло не так — объяснить проблему понятно
- Что работает нормально — хорошие новости для баланса
- Контекст — что ещё важно знать

**Так писать нельзя:**
- Сухие статусы без объяснений: «Шаг 1: FAIL, Шаг 2: ERROR»
- Сущности кода без пояснения: «Проверил `SessionManager.get_active_session()`»
- Аббревиатуры без расшифровки
- Формальные обороты: «В ходе анализа было установлено, что...»

### 7.1 Формат отчёта

Используй **строго** этот шаблон (структура разделов обязательна, но текст внутри —
в дружелюбном разговорном стиле с пояснением каждой сущности):

```markdown
# Анализ первопричины: [краткое, понятное название проблемы]

**Дата анализа:** DD-MM-YYYY
**Проект:** [название проекта из CLAUDE.md]
**Статус:** [Первопричина найдена / Требуется дополнительное исследование]

## Симптом

Расскажи простыми словами, что именно сломалось — так, чтобы человек без знания кода
понял ситуацию. Каждую сущность кода поясняй при первом упоминании.

## Цепочка причин

Пошаговая цепочка от симптома до корневой причины. Каждое звено — понятным языком,
с пояснением технических деталей в скобках:

1. **[Симптом простыми словами]** → потому что →
2. **[Промежуточная причина — с пояснением что это]** → потому что →
3. **[Ещё глубже — что именно пошло не так]** → потому что →
4. **[КОРНЕВАЯ ПРИЧИНА — почему так получилось]**

## Корневая причина

Расскажи подробно, в чём корень проблемы. Укажи конкретные файлы и строки, но поясни
что каждый файл делает и за что отвечает. Объясни почему именно это — корень, а не
промежуточное звено.

## Почему проблема не была предотвращена

Расскажи, какие процессы и инструменты должны были поймать эту проблему раньше —
и почему не поймали.

### Проверенные скиллы

- `skill-name` (что делает этот скилл) — [есть/нет] пункт
  про [что именно]. [Вывод]

### Проверенные документы

- `path/to/doc.md` (что это за документ) — [что нашли / не нашли]

## Рекомендации по исправлению

Конкретные шаги, чтобы починить проблему:

1. [Что сделать, в каком файле (пояснение что это за файл), какой код изменить]
2. [Следующий шаг]

## Рекомендации по предотвращению

Что изменить, чтобы такое больше не повторялось:

1. [Конкретное изменение и почему оно важно]
2. [Следующее изменение]

## Верификация выводов

- Какие документы перечитал повторно и что увидел свежим взглядом
- Какие альтернативные объяснения рассмотрел и почему они менее вероятны
- Подтвердилась ли корневая причина или пришлось пересмотреть

## Верификация решений

- **Рекомендация:** [краткое описание]
  - **Вердикт:** ОДОБРЕНО / ОДОБРЕНО С ЗАМЕЧАНИЯМИ / ОТКЛОНЕНО
  - **Обоснование:** [почему такой вердикт]
  - **Корректировка:** [была ли изменена — или «без изменений»]

## Чек-лист исправлений

### Изменения в документацию [DOC]
- [ ] Описание изменения (файлы: путь/к/документу.md, зачем: причина)

### Изменения в коде [CODE]
- [ ] Описание изменения (файлы: путь/к/файлу, зачем: причина)

### Изменения в скиллах [SKILL]
- [ ] Описание изменения (скилл: имя-скилла, зачем: причина)
```

### 7.1.1 Обязательная проверка: нужно ли обновить CLAUDE.md?

После написания раздела "Рекомендации по предотвращению" в отчёте — ОСТАНОВИСЬ и ответь
на вопрос: **является ли корневая причина отсутствием архитектурного правила или принципа?**

Признаки архитектурной корневой причины:
- Проблема повторяется в разных модулях
- Агент принял решение, которое не описано ни в одном документе (и выбрал неверно)
- Несколько скиллов пришлось обновить одной и той же "заплаткой"
- Вопрос "когда/зачем/нужно ли" не имеет ответа в `CLAUDE.md`

**Если ДА — корневая причина архитектурная:**

1. Сформулируй ПРИНЦИП (не заплатку):
   - Плохо: "Добавить Step 4 проверки в скилл реализации"
   - Хорошо: "Компоненты X и Y координируются через механизм Z. Записать в CLAUDE.md"

2. Укажи ТОЧНОЕ место в `CLAUDE.md`, куда вставить принцип:
   - Раздел (например: "Архитектурные принципы")
   - После какой строки или перед каким заголовком

3. Пометь рекомендацию тегом по типу изменения:
   - `[ARCHITECTURE]` — обновить CLAUDE.md
   - `[DOC]` — обновить документацию (спецификации, гайды)
   - `[CODE]` — исправить код
   - `[SKILL]` — обновить скилл (ссылкой на CLAUDE.md, не дублированием правила)

4. Порядок приоритетов в рекомендациях по предотвращению:
   `[ARCHITECTURE]` → `[DOC]` → `[CODE]` → `[SKILL]`.
   Сначала архитектурные принципы (фундамент), затем документация,
   потом код, и в последнюю очередь скиллы:
   ```
   ## Рекомендации по предотвращению

   1. [ARCHITECTURE] Добавить в CLAUDE.md раздел "X": {формулировка принципа}
      Место: после раздела Y, перед разделом Z

   2. [DOC] Обновить спецификацию модуля: добавить описание {чего не хватало}
      Файл: {путь к спецификации}

   3. [CODE] Исправить {что именно} в {каком файле}

   4. [SKILL] В скилле {имя-скилла} добавить ссылку:
      "Перед реализацией см. раздел X в CLAUDE.md"
   ```

**Если НЕТ — корневая причина не архитектурная:**

Это нормально — не каждая проблема требует архитектурного принципа. Продолжай
с рекомендациями по обновлению скиллов.

**Правило:** рекомендации по предотвращению должны включать обновление
CLAUDE.md, если проблема архитектурная. Обновление только скиллов —
недостаточно, потому что скиллы — это инструкции "как делать", а CLAUDE.md —
знание "как устроена система". Агент читает CLAUDE.md первым.

### 7.2 Сохранение отчёта

1. Узнай текущую дату и время: `date +"%d-%m_%H-%M"`
2. Сформулируй краткое описание проблемы для имени файла — 2-4 слова через дефис
3. Определи папку для сохранения:
   - Если в аргументах указан `output_dir=путь/к/папке` — использовать эту папку
   - Иначе — проверь `~/.claude/references/document-naming-and-placement.md` для
     правил размещения документов и следуй стандарту проекта
   - Если стандарт не определяет место для root-cause отчётов — создай папку
     `root-cause-reports/` внутри папки документации проекта (обнаруженной в Фазе 0)
4. Создай директорию если нет: `mkdir -p {выбранная папка}`
5. Имя файла: `{DD-MM_HH-MM}_краткое-описание-проблемы.md`
6. Сохрани через Write

### 7.3 Итог

Кратко сообщи пользователю:
- Какая корневая причина найдена (1-2 предложения)
- Путь к отчёту
- Самые важные рекомендации по предотвращению

---

## Сессионный отчёт

Запусти агента для создания сессионного отчёта (обязательно для пайплайнов, правило из AGENTS.md).

Промпт агенту:
```
Используй /session-report.
Контекст: пайплайн root-cause-analysis завершён.
Проблема: {описание проблемы}
Путь к отчёту: {путь к root-cause отчёту}
```

## Важные правила

- **Фаза 0 не пропускается** — каждый запуск начинается с разведки проекта,
  даже если ты «думаешь, что знаешь» проект. Никаких предположений о структуре
- **Не останавливайся на поверхности** — «пакет не установлен» это не корневая причина.
  Корневая причина — почему пакет не оказался в зависимостях
- **Читай файлы целиком** — контекст вокруг ошибки часто важнее самой ошибки
- **Проверяй скиллы обязательно** — это уникальная возможность найти системные пробелы
  в процессах, а не просто одноразовый баг
- **Двойная проверка обязательна** — фаза верификации не пропускается никогда
- **Конкретика, не абстракции** — «файл X, строка Y, функция Z» вместо «в одном из файлов»
- **Глобальные стандарты — по ссылке** — не копируй содержимое из `~/.claude/references/`
  в скилл, а читай при каждом запуске. Стандарты могут обновляться
